# D10 MVP 验收记录

| 项目 | 内容 |
| --- | --- |
| 执行日期 | 2026-08-08 |
| 代码基线 | `16864ab7f8bcf84e62a99ff54c8c24079c44c9ff`，叠加本次未提交的验收修复 |
| 验收环境 | Windows、Docker 29.6.1、Docker Compose 5.3.0、独立项目 `supplier-risk-d10` |
| 当前结论 | **D10 技术发布验收通过**：隔离空库迁移、OFAC 真实采集/去重、自动化回归和无密钥启动均已完成；OFAC 使用条款仍需业务方复核 |

## 已通过项目

| 验收项 | 结果与证据 |
| --- | --- |
| 后端全量测试 | `pytest -q`：125 项通过；仅有 Starlette TestClient 弃用警告 |
| 后端静态检查 | Ruff 全部通过；mypy 严格模式检查 55 个源文件，无问题 |
| 前端测试 | Vitest 1 个文件、3 项测试通过 |
| 前端类型与构建 | `tsc --noEmit` 通过；Vite 生产构建成功，转换 446 个模块 |
| Docker 镜像构建 | Dockerfile 内再次执行前端测试、类型检查和生产构建，全部通过 |
| 空库迁移 | 独立空数据卷从 `0001` 连续迁移到 `0012 (head)`；app、scheduler 当前均报告 `0012 (head)` |
| 单端口启动 | `127.0.0.1:18080` 健康检查为 `ok`，主页 HTTP 200，OpenAPI 共 26 个路径 |
| 无密钥启动 | 隔离环境强制 Fake AI，AI Key 和 Base URL 为空；未输出真实配置内容 |
| 八个端到端场景 | `tests/test_acceptance.py`：8 项全部通过 |
| 100 家 Excel 导入 | 通过真实 HTTP 接口一次导入 100 家供应商，每家包含生产地点和供应产品 |
| 数据持久化 | 重启 PostgreSQL、app、scheduler 后，100 家供应商仍可查询 |
| 天气真实采集 | 中央气象台实际拉取 100 条、创建 100 条、0 重复、无错误 |
| OFAC 适配器 | 官方 CSV 字段、302 重定向、DOS EOF 尾行、HTTP 异常、短行、标准化、指纹和健康检查测试已补齐；真实采集首轮抓取 19,199 条、创建 19,199 条、0 重复、无错误；重复采集抓取 19,199 条、创建 0 条、重复 19,199 条、无错误 |
| 真实模型记录 | 当前正式本机实例可见 100 条 `openai-compatible` 成功分析记录 |
| Git 密钥扫描 | 当前提交及全部 Git 历史未命中常见真实密钥格式；`.env` 未被跟踪 |
| 备份恢复 | PostgreSQL 自定义格式备份恢复到隔离数据库；恢复后供应商 100、风险信号 100 |
| UI 操作验收 | 总览、当前风险、详情、供应商、数据源均可操作；详情包含来源、证据和评分；浏览器控制台 0 个错误 |

## 八个端到端场景

1. 多地点、多产品供应商导入与查询通过。
2. 天气事件地理匹配并生成高等级提醒通过。
3. 法人名称和注册编号主体匹配通过。
4. 仅国家弱信号不能生成 P1 通过。
5. 两来源转载只形成一个事件和一条当前提醒通过。
6. AI 超时重试且最终失败不生成无依据提醒通过。
7. 非法 Excel 返回逐行错误且不破坏已有数据通过。
8. 风险详情包含原始来源、匹配理由和评分明细通过。

## 本次修复

- 删除当前风险页面中硬编码的风险指数、P1/P2/P3 数量和虚假分页，改为展示真实加载数据的 P1 至 P4 数量。
- 删除尚未接入真实数据的国家、失效筛选控件，避免提供无效操作。
- 修正风险详情中的等级文案重复和证据类型误标。
- OFAC 官方 CSV 客户端跟随 302 重定向，并忽略文件末尾 DOS EOF 标记。
- 采集通用入库改为每批 1,000 条，避免 PostgreSQL 参数上限阻断大名单采集。

## 发布门结论

技术方案要求同时具备：天气/自然灾害真实来源、企业公告/监管/政策/制裁类真实来源，
以及手工导入来源。此前数据库只有：

1. `manual-json` 标准 JSON 手工导入；
2. `nmc-weather` 中央气象台天气预警。

天眼查当前 `tyc_enabled=False`，且现有实现是 Agent 核查工具，不是定时采集的
`SourceAdapter`。本轮已实现 OFAC SDN `SourceAdapter`，并在独立全新数据库中完成真实采集和指纹去重：

- 首轮运行 ID 68：抓取 19,199，创建 19,199，重复 0，状态 `succeeded`。
- 重复运行 ID 69：抓取 19,199，创建 0，重复 19,199，状态 `succeeded`。
- 采集失败会写入 `collection_runs`；本次修复前的失败记录保留在隔离库中，最新成功记录无错误。
- 隔离环境强制 Fake AI，`AI_BASE_URL`、`AI_MODEL`、`AI_API_KEY` 均为空。

以下技术发布条件均已满足；法律/使用条款由业务方在目标部署前复核：

- OFAC SDN 使用官方公开 CSV 接口，部署前仍应由业务方按目标部署区域复核使用条款；
- 隔离环境实际采集成功，失败处理、指纹去重和采集运行记录均已验证；
- 后端全量测试 125 项、八个端到端场景、空库迁移和 Docker 健康检查均已完成。

## 已知非阻断限制

- 当前风险接口一次最多加载 100 条，页面明确显示“已加载结果”；MVP 尚未实现服务端分页。
- 本机 Vite 在受限沙箱内无法写入 `node_modules/.vite-temp`，使用本机正常权限执行时测试和构建均通过；未重装依赖。
- 系统仅绑定 `127.0.0.1`，本机休眠或 Docker 停止时不会继续监控。

## 数据源智能接入真实链路验收（2026-08-10，USGS）

首个通过"探测 → 预览 → 草稿 → 发布 → 启用 → 采集 → AI 解析 → 规则引擎"全链路的真实公开 HTTPS 来源：**USGS 全天地震速报**（`https://earthquake.usgs.gov/earthquakes/feed/v1.0/summary/all_day.geojson`，官方 GeoJSON，免认证，数据源 ID 49，编码 `usgs-earthquake-day`，可信度 95，周期 `*/30 * * * *`）。

- 探测：平台 `inspect_source_url` 正确识别 JSON 格式与结构。
- 实时预览：10 条标准化样例（`features` → `id`/`properties.title`/`properties.place`/`properties.url`），不落库。`properties.time` 为 epoch 毫秒，与平台"ISO 8601 带时区"要求不兼容，故 `published_at` 不映射，指纹字段为 `external_id + title`。
- 发布门：管理员确认后发布，adapter_version=1，保持停用；启用为独立管理员操作。
- 正式采集：run 1688 抓取 256、创建 256、重复 0；run 1689 抓取 256、创建 0、重复 256（指纹去重生效）。
- AI 解析与引擎：抽样 3 条信号全部解析成功，归并为 geological/geological_hazard 事件并生成中文摘要；加州低烈度地震对国内供应商零匹配零提醒（无误报）。
- 负向验收：用户指定的 `data.earthquake.cn` 速报页为 Wabacus 框架 JS 动态渲染且 WAF 拦截平台请求（403），平台按设计返回"需要开发扩展"并对域名熔断 30 分钟——动态页面识别与访问治理均按预期工作。
- 环境备注：本机代理 fake-ip DNS（198.18.0.0/16）会导致容器内 SSRF 校验拒绝解析结果；本轮在容器 /etc/hosts 写入真实 IP 完成验收（重启后失效）。生产部署无此问题；本地长期方案是在 compose.yaml 为容器配置公共 DNS。
- 遗留：USGS 剩余 253 条信号与 nmc 积压 2205 条由 Scheduler 按每批 20 条节奏消化；草稿箱中两条历史测试用接入中草稿可由管理员用新删除功能清理。
