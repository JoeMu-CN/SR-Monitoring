# 数据源智能接入与实时查询实施计划

> **2026-08-11 定位说明**：本文记录既有声明式数据源接入能力及其验收事实。JSON、CSV 和服务端渲染静态 HTML 的受控接入继续维护；不再继续深挖动态网页、反爬、验证码、任意站点或通用信息研究。RSS/RSSHub、Crawl4AI 与自主研究轨的后续工作统一由[《智能信源采集与分析落地执行计划》](智能信源采集与分析落地执行计划.md) v1.2 管理，目前仅批准研究轨开发阶段0技术 Spike；ECS 旁路部署、真实任务、容量和 48 小时观察属于独立的 ECS部署验证阶段0。

## 1. 目标

让平台管理员在网页内通过内置 Agent 完成结构化数据源接入，无需登录服务器修改代码。Agent 根据管理员提供的官方入口和监控目标生成声明式适配器配置，平台在受控网络边界内执行实时预览、测试、保存和正式采集。

本阶段交付最小可用闭环：

1. 管理员通过平台 Agent 描述数据源。
2. Agent 调用白名单工具创建适配器草稿。
3. 平台按草稿实时联网获取少量样例并展示标准化结果。
4. 管理员确认后发布配置；新配置默认停用。
5. 管理员启用后，可手动采集并纳入现有 Scheduler、去重和风险分析链路。
6. 所有创建、测试、发布和启停操作保留审计记录。

## 2. 范围与取舍

### 2.1 本阶段支持

- HTTPS 官方数据源。
- JSON API、CSV 文件和服务端渲染的 HTML 列表页。
- GET 请求、静态查询参数和有限分页。
- `none`、API Key Header 和 Bearer Token 三种认证表达。
- API Key 仅使用外部凭据引用；不向 Agent 返回明文凭据。
- 字段映射到现有标准信号：`external_id`、`title`、`content`、`url`、`published_at`。
- 稳定字段组合生成 SHA-256 指纹。
- 实时连接测试、标准化预览、保存草稿、发布、启停和手动采集。
- 复用现有 `collection_runs`、`raw_signals`、Scheduler 和风险处理链路。

### 2.2 本阶段不支持

- Agent 生成或执行任意 Python、Shell、SQL。
- JavaScript 浏览器自动化、XHR 监听、验证码和复杂登录。
- OAuth2 授权码交互、自定义签名算法和厂商 SDK。
- PDF/OCR、跨页面关联和复杂爬虫。
- 未经管理员确认自动发布或自动启用。
- 访问 HTTP、本机、内网、云元数据地址或非审批域名。

超出声明式能力的数据源必须返回“需要开发扩展”，不得降级为任意代码执行。

### 2.3 网页自发现既有能力维护

既有“先给官方地址、再由 Agent 探测”的接入流程继续维护：

- Agent 先调用受控 URL 探测工具，识别响应类型、页面标题、可见文本和候选列表结构。
- 对服务端渲染的静态 HTML，Agent 生成声明式选择器配置，平台使用确定性解析器预览和采集。
- 对网页背后的公开 JSON/XHR 请求，只记录诊断线索；不再由本接入助手扩展浏览器网络发现或点击脚本。
- 动态 JavaScript 渲染、登录、验证码、OAuth、PDF/OCR 和跨页面关联继续返回“需要开发扩展”。

维护目标是保证已交付的“探测 → 生成配置 → 实时预览 → 草稿”闭环稳定；发布、启用和正式采集仍沿用现有管理员确认门。超出声明式边界的来源转交独立监控连接器或研究轨评估，不在写入型接入助手中混入通用抓取工具。

## 3. 安全边界

1. 仅管理员可调用接入类 Agent 工具。
2. 实时联网仅允许 HTTPS，解析后拒绝 loopback、私网、链路本地、保留地址和云元数据地址。
3. 重定向后的每个目标继续执行相同校验；限制重定向次数。
4. 单次请求限制连接/读取超时、响应大小、最大条目数和最大分页数。
5. 凭据只通过环境变量名或外部密钥引用解析，禁止存入适配器 JSON、日志和 Agent 上下文。
6. 新建适配器状态为草稿；发布后数据源仍默认停用。
7. 实时预览不写入 `raw_signals`，正式采集才进入现有证据链。
8. 所有配置保存版本号；失败可停用并恢复上一发布版本。

## 4. 数据模型

在 `data_sources` 增加声明式适配器字段：

- `adapter_config`：JSONB，保存请求、解析、映射和指纹配置，不保存密钥。
- `adapter_status`：`draft`、`published` 或 `invalid`。
- `adapter_version`：整数，每次发布递增。
- `published_at`：最近发布时间。

首版不单独建立版本历史表；配置变更继续写入现有 `data_source_audit_logs`，审计记录保存脱敏后的新旧版本摘要。出现正式回滚需求时再增加不可变版本表。

## 5. 声明式适配器格式

```json
{
  "format": "json",
  "request": {
    "method": "GET",
    "url": "https://official.example/api/events",
    "params": {},
    "headers": {},
    "timeout_seconds": 15,
    "max_response_bytes": 10485760
  },
  "items_path": "data.items",
  "mapping": {
    "external_id": "id",
    "title": "title",
    "content": "description",
    "url": "official_url",
    "published_at": "published_at"
  },
  "fingerprint_fields": ["external_id", "title", "published_at"]
}
```

CSV 使用列名作为映射路径；JSON 首版使用简单点号路径，不引入 JSONPath 依赖。
HTML 使用 `items_selector` 选择列表项，映射字段使用相对于列表项的 CSS 子集选择器；链接字段可使用 `selector@href` 读取属性。

## 6. 平台 Agent Skill 与工具

接入 Skill 作为平台内置的流程提示词，不作为可执行代码。触发接入意图后，引导 Agent：

1. 收集官方 URL、数据格式、监控目标和授权说明。
2. 生成符合 Schema 的适配器草稿。
3. 调用实时预览工具验证连通性和字段映射。
4. 根据错误修正草稿，最多进行有限次数迭代。
5. 向管理员展示来源、映射、样例、丢弃条目和安全限制。
6. 经管理员明确确认后保存或发布；不得自主启用。

新增白名单工具：

- `inspect_source_url`：受控读取官方 HTTPS 地址，识别 JSON/CSV/HTML 并返回脱敏的结构摘要，不落库。
- `create_source_adapter_draft`：校验并保存草稿。
- `preview_source_adapter`：受控联网，返回少量标准化样例和诊断信息，不落业务数据。
- `publish_source_adapter`：管理员确认后发布配置，仍保持停用。
- `run_source_now`：对已发布且已启用来源执行一次正式采集。

查询类 Agent 工具继续只读；接入工具必须同时校验管理员角色和数据源状态。

## 7. 实现阶段

### 阶段 A：声明式运行时

- 定义 Pydantic AdapterSpec。
- 实现 HTTPS/SSRF 校验、响应大小限制和 JSON/CSV 解析。
- 实现字段映射、日期解析、标准化和稳定指纹。
- 将声明式适配器接入现有 `collect_source`。
- 为协议、异常、去重和安全边界补测试。

### 阶段 B：管理接口与实时预览

- 增加迁移和数据源读写 Schema。
- 增加草稿保存、实时预览和发布接口。
- 数据源页面增加 AdapterSpec 编辑、预览、诊断和发布操作。
- 保留管理员权限与审计。

### 阶段 C：Agent Skill 和工具

- 增加接入 Skill 提示词。
- 增加四个接入白名单工具和参数 Schema。
- 对接 Agent 编排，接入意图使用管理员工具集。
- 测试非管理员拒绝、密钥不泄漏和发布确认门。

### 阶段 D：调度与验收

- Scheduler 构建适配器时优先读取已发布声明式配置。
- 支持手动立即采集；Scheduler 每分钟自动刷新已发布/内置来源的调度注册表。
- 使用 MockTransport 完成自动化测试。
- 经用户许可后选择一个公开 HTTPS 来源进行真实连接测试。

### 阶段 E：网页自发现第一阶段（既有能力维护）

- 扩展 `AdapterSpec` 支持 `html` 格式和有限 CSS 选择器配置。
- 新增受控 URL 探测：只允许公网 HTTPS、禁止重定向和内网地址，限制响应大小和返回文本长度。
- Agent 先探测地址，再生成 HTML 选择器适配器，调用同一实时预览工具验证标准化结果。
- 只支持服务端渲染 HTML；页面结构不满足条件时明确返回需要开发扩展。
- 补充 HTML 解析、选择器、超限响应、SSRF 和 Agent 工具白名单测试。

### 阶段 F：网页自发现第二阶段（停止在本计划内推进）

- 不再为写入型数据源接入助手增加隔离浏览器、动态点击、通用内容提取 DSL 或反爬能力。
- 需要 JavaScript 单页读取或公开信息研究的场景，按 v1.2 总计划在独立研究轨中做受限 Spike；研究工具不得获得发布、启用或正式采集数据源的权限。
- 登录、验证码、付费墙、WAF、反爬和未授权来源继续明确拒绝。

## 8. 验收标准

1. 管理员能在平台内创建 JSON、CSV 或静态 HTML 数据源草稿。
2. 实时预览能联网返回标准化样例且不写入 `raw_signals`。
3. 非 HTTPS、内网、超大响应、非法映射和未授权操作被明确拒绝。
4. Agent 和接口响应不包含任何明文密钥。
5. 发布不会自动启用；管理员启用后手动采集可生成 `collection_runs` 和去重后的 `raw_signals`。
6. 中央气象台、OFAC 和手工 JSON 现有行为不回归。
7. Backend pytest、Ruff、mypy、Alembic 升降级检查通过；前端测试、ESLint 和生产构建通过。
8. 输入一个授权的静态 HTML 官方地址时，Agent 能返回探测摘要并生成可预览的选择器配置；动态页面明确提示需要开发扩展。

## 9. 部署边界

本功能涉及后端、前端和数据库迁移。代码验收完成后需要重建应用与 Scheduler 镜像并执行 Alembic 迁移。未经用户明确要求，本开发过程不操作生产凭据、不执行真实外部采集、不发布或启用任何新来源。

2026-08-11 后，本计划只接受既有声明式能力的安全、缺陷和兼容性维护。RSSHub、Crawl4AI、搜索 Provider 与研究 Worker 不并入数据源接入助手的写入工具集；其隔离部署、域名治理和阶段审批以 v1.2 总计划为准。

## 10. 当前实施状态（2026-08-09）

- 阶段 A-C 和阶段 D 的自动化部分已完成。
- Alembic `0014` 已执行；本地开发数据库当前为后续迁移 `0015 (head)`。
- 后端完整 pytest、Ruff、mypy，以及前端 Vitest、TypeScript 和生产构建已通过。
- 未执行真实外部数据源采集；仍需选择一个已确认授权的公开 HTTPS 来源完成现场验收。
- 本地 Docker 应用镜像重建受 Docker Hub 授权端点网络超时阻塞，当前运行容器仍使用重建前镜像。

## 11. 风险查询 Agent 与数据源接入 Agent 拆分

为避免查询工具与配置写入工具在同一入口、同一会话中互相干扰，平台拆分为两个独立 Agent：

1. **风险查询 Agent**：独立导航入口；仅使用供应商、当前风险、企业核查和额度查询工具，任何角色下都不加载数据源配置工具。
2. **数据源接入 Agent**：独立导航入口；仅管理员可调用；只加载实时预览、创建草稿、发布和立即采集工具，不加载风险查询工具。
3. 两类会话在 `agent_sessions.agent_kind` 中分别标记为 `risk_query` 和 `source_onboarding`；服务端拒绝跨类型复用会话 ID。
4. 保留底层 LLM/ReAct 执行器复用，但使用独立系统提示、工具白名单、API 路由和前端会话状态。
5. 风险查询入口继续使用 `/api/v1/chat`；数据源接入入口使用 `/api/v1/source-agent/chat`，后者强制管理员权限。
6. 前端分别显示“风险查询 Agent”和“数据源接入 Agent”按钮；数据源接入页面明确展示草稿、发布、启用和采集的确认边界。

验收重点：风险查询 Agent 永远看不到写入工具；数据源接入 Agent 永远看不到供应商/风险查询工具；跨 Agent 会话 ID 返回冲突错误；两个入口可独立重置和持续多轮对话。

## 12. 阶段 E 实施状态（2026-08-09）

- `AdapterSpec` 已支持 `html` 格式、`items_selector` 和有限 CSS 选择器字段映射。
- 新增 `inspect_source_url` 接入工具：受控探测公网 HTTPS，识别 JSON/CSV/HTML，返回页面标题、可见文本摘要和候选选择器，不落库。
- 静态 HTML 已接入实时预览、草稿、发布和正式采集链路；链接字段支持 `selector@href`。
- 仍拒绝重定向、内网地址、超大响应和不支持的选择器；动态 JavaScript/XHR、登录、验证码、PDF/OCR 继续返回需要开发扩展。
- 本轮完整后端测试、Ruff、mypy 和前端 Docker 构建门禁已通过；未执行真实外部网页采集。

## 13. 阶段 G：外部源访问风控（2026-08-09 追加）

真实官方站点可能配置 WAF、IP 信誉、请求频率或并发限制。探测成功不代表允许平台持续抓取，接入、预览、发布验证和定时监听必须共用同一套域名访问治理：

1. 以域名而不是单个数据源为隔离单位，Web 与 Scheduler 通过 PostgreSQL 共享租约和冷却状态。
2. 同一域名只允许一个在途请求，两次请求至少间隔 10 秒；进程异常后租约自动过期。
3. 403 或识别到安全盾页面后立即熔断 30 分钟；429 优先遵循 `Retry-After`，否则冷却 15 分钟。
4. 网络错误和 5xx 不立即重试，按连续失败次数进入 1、2、4 分钟直至最多 30 分钟的递增冷却。
5. Agent 遇到访问受限后不得继续调用同域名的探测、预览、发布或采集工具，不得伪装浏览器、更换 IP 或绕过验证码/WAF。
6. 数据源管理页显示域名保护状态、最近 HTTP 状态和冷却截止时间；冷却、限速或已有请求时禁用连通性测试。
7. 自动化验证只使用 MockTransport 和独立测试数据库，不再以真实站点承受重复请求作为常规验收方式。

本阶段不引入 Redis、消息队列或浏览器自动化。后续只有在多节点吞吐量确有需要时，再把 PostgreSQL 租约替换为专用分布式限流服务；站点级自定义间隔和详情页小批量队列另行实施。

## 14. 阶段 H：逐项接入问答与草稿箱（2026-08-10 追加）

### 14.1 目标

将数据源接入助手从一次性笼统对话改为单问题、可恢复的引导式接入。管理员无需事先了解适配器字段；助手每次只收集一个必要信息，并把半途输入安全地保存到草稿箱。

### 14.2 引导步骤

1. 官方 HTTPS 地址。
2. 需要采集的字段与业务目标。
3. 公开/授权条件及认证方式；只接受凭据引用，禁止输入明文密钥。
4. 数据源名称、稳定编码和采集周期。
5. 探测、字段映射、实时预览和生成正式适配器草稿。

助手在每一步只提出一个清晰问题，并说明推荐填写方式；收到访问受限、验证码、未授权或超出声明式能力的答案时停止联网并保存当前草稿，不绕过安全防护。

### 14.3 草稿箱模型与边界

- 新增持久化的“接入中草稿”，保存当前步骤、脱敏回答、所属管理员和关联会话；它尚不是 `data_sources`，不能发布、启用或采集。
- 生成正式 `DataSource` 后，接入中草稿关联其数据源 ID，不再重复列出。
- 草稿箱同时聚合：接入中草稿、现有 `adapter_status=draft` 的适配器草稿，以及已发布但尚未启用的数据源。
- 适配器配置、审计记录和凭据仍只保存在既有唯一数据源记录中；草稿箱不复制明文凭据、不自动发布、不自动启用。

### 14.4 验收标准

1. 管理员进入接入助手即看到第一道问题和进度；每次发送答案后只收到下一道问题或明确的验证结论。
2. 中途退出并重新进入后可恢复同一草稿和已填答案。
3. 草稿箱能清楚区分接入中、待发布和待启用，并显示关联数据源 ID/编码。
4. 非管理员不能创建、读取、恢复、删除草稿或改变数据源启停状态。
5. 半途草稿、已发布未启用项均不能触发正式采集；现有 WAF/429 访问治理继续生效。

### 14.5 实施状态（2026-08-10）

- 逐项问答状态机、`SourceOnboardingDraft` 模型、迁移 `0018`、草稿箱聚合接口与前端接入助手页面已完成并容器内验证。
- 本轮补齐验收缺口：新增 `DELETE /api/v1/source-agent/drafts/{draft_id}`（仅管理员；已生成正式数据源的草稿返回 409）；前端草稿箱“接入中”条目支持删除；`api.ts` 支持 204 空响应。
- 新增测试：断点恢复不推进步骤、草稿会话不匹配拒绝、已完成草稿拒绝恢复、已发布未启用拒绝采集、删除权限/404/409 全流程、草稿箱 in_progress 聚合。
- 门禁：后端 161 项 pytest 全过、Ruff 全过、mypy 60 文件 0 错；前端 tsc + Vite 构建通过，产物已同步 `backend/app/static` 与运行容器。
- 运行时冒烟：健康检查通过；删除接口非管理员 403、不存在草稿 404 符合预期。
- 现场验收已完成（USGS 全链路，见 §15.1）；data.earthquake.cn 负向验收结论与卡点见 §15.2。

## 15. 2026-08-10 后续：真实验收、运行环境修复与能力增强

### 15.1 真实信源全链路验收（USGS，通过）

首个通过"探测 → 预览 → 草稿 → 发布 → 启用 → 采集 → AI 解析 → 规则引擎"全链路的真实公开 HTTPS 来源：**USGS 全天地震速报**（`https://earthquake.usgs.gov/earthquakes/feed/v1.0/summary/all_day.geojson`，官方 GeoJSON 免认证，数据源 ID 49，编码 `usgs-earthquake-day`，可信度 95，周期 `*/30 * * * *`）。

- 探测识别 JSON；实时预览 10 条标准化样例不落库。
- `properties.time` 为 epoch 毫秒，与平台"ISO 8601 带时区"要求不兼容 → `published_at` 不映射，指纹字段 `external_id + title`。
- 发布后保持停用，启用为独立管理员操作（确认门全部生效）。
- 正式采集：run 1688 抓取 256、创建 256、重复 0；run 1689 抓取 256、创建 0、重复 256（指纹去重生效）。
- AI 解析抽样 3 条全部成功，归并为 geological/geological_hazard 事件并生成中文摘要；加州低烈度地震对国内供应商零匹配零提醒（无误报）。

### 15.2 data.earthquake.cn 负向验收与判断更正

- **判断更正**：该速报页实为服务端渲染（`tr[global_rowindex]` 数据行含发震时刻），此前"纯 JS 动态渲染"结论错误。
- 真实卡点：① 所有数据列共用 `cls-data-td-list` class，声明式选择器引擎无 nth-of-type/位置选择，无法区分"时刻/震级/地点"列（解法则需给选择器加位置支持）；② 站点 WAF 拦截平台请求（403 access_blocked），域名按阶段 G 规则熔断 30 分钟——治理机制按设计工作。
- 结论：该源在"选择器位置支持 + 非 WAF 拦截网络"条件下可接入；当前判定为需要开发扩展并保留页面快照 `data/eq_probe.html` 作为后续增强验证样本。

### 15.3 运行环境修复（容器 DNS）

- 本机代理 fake-ip DNS（198.18.0.0/16）污染容器解析，导致 SSRF 校验把公网域名误判为非公网地址、AI 接入域名（Aliyun MaaS）超时挂起。
- compose.yaml 给 app/scheduler 配置公共 DNS `223.5.5.5 / 119.29.29.29`，重建后验证：全部域名解析真实公网 IP，平台探测免 hosts 直连成功，LLM 调用恢复正常。
- 开发经验：`docker compose up -d` 重建容器会丢失 docker cp 的代码，重建后需整目录重新同步 `app/`、`tests/`、`alembic/versions/`。

### 15.4 信号处理优化（批大小可配置 + 解析前相关性预过滤）

- `SIGNAL_ANALYZE_BATCH`（默认 20，范围 1-500）：每次定时任务对积压信号执行 AI 解析的批大小，可经环境变量调整，无需改代码。
- 新增 `app/signals/relevance.py`：LLM 解析前的确定性相关性预过滤（保守原则，拿不准一律放行）：
  1. 命中供应商主体/别名 → 放行（强相关）；
  2. 命中中国地名且存在中国供应商 → 放行；
  3. 命中国外地理（国家全名/美国州名/美州缩写 `, XX` 边界）且这些国家均无供应商 → 过滤；
  4. 其余 → 放行。
- scheduler 在调 LLM 前先过滤；被过滤信号写 `provider=deterministic-filter` 的 succeeded 解析记录（不重复取出、不消耗 token）。开关 `SIGNAL_RELEVANCE_FILTER_ENABLED` 默认开启。
- 已知限制：`raw_data` 是标准化信号不含原始经纬度，坐标地理围栏留待 adapter 保留原始坐标后增强。
- 开发阶段按用户决定清空积压（21702 条未解析信号），保留已成功解析信号及其事件链。

### 15.5 接入 Agent 能力增强（独立于 workbuddy 应对陌生数据源）

目标：平台管理员在平台页面使用接入助手即可完成陌生数据源接入，无需在项目工作目录外部验证。

1. **已验证信源目录** `VERIFIED_SOURCE_CATALOG`：只收录经平台全链路验证的官方信源（首条 USGS GeoJSON）；用户地址失败时优先推荐目录内同维度源，推荐前必须先 `inspect_source_url` 验证，禁止凭记忆推荐目录外地址（消除模型幻觉）。
2. **inspect 探测增强**：HTML 响应新增 `rendering`（server_rendered/likely_dynamic）、`script_sources`（同域脚本）、`endpoint_hints`（页面内疑似数据接口）；动态页面返回线索而非直接拒绝。
3. **架构能力矩阵决策树**（提示词注入）：探测一次即归类，不盲目试错——
   (1) JSON/CSV → 直接适配；(2) HTML server_rendered 可定位 → 选择器适配；
   (3) HTML likely_dynamic → 验证 endpoint_hints 找 JSON 接口改用 JSON 适配器；
   (4) JS 执行/登录/验证码/OAuth/POST 复杂签名/PDF → 立即判定需要开发扩展并一句话说明子类型。

### 15.6 待办

- 任务 9：保留天数改为平台管理员可配置——**暂缓**，待角色权限功能开发完成后决定管理员需要能配置哪些系统选项。
- 选择器位置支持（nth-of-type）：提升分类(2)的列结构覆盖，可解 data.earthquake.cn 列区分（低优先）。
- 原阶段 F（浏览器沙箱/网络请求发现）不再由本接入助手推进；单页读取验证转入 v1.2 总计划的研究轨开发阶段0，ECS 真实旁路验证另属 ECS部署验证阶段0。
- 宏观数据源（商务部制裁清单、天气预警等）接入：配合信源目录持续扩充与验收。

## 16. 浏览器沙箱方案评估结论（2026-08-10）

### 16.1 能力边界

- **能治**：第④类中"数据必须执行 JavaScript 才出现"的页面。正确路径为阶段 F 设计——沙箱只渲染页面并观察 Fetch/XHR，发现真实接口后回落 JSON 适配器，不默认执行浏览器点击。
- **治不了**：登录/OAuth/验证码（授权边界，非技术手段问题）；自定义签名接口（需开发扩展）；PDF/OCR（另一子系统）。
- **可能更差**：WAF/反爬。headless 浏览器携带 CDP 指纹特征，反爬识别率高于普通请求头，403 问题可能加重而非缓解。

### 16.2 引入代价

- 每源一个浏览器实例，内存/CPU 为真实容量压力（单 Chromium 实例约 200-500MB），与 ECS 容量预算直接冲突。
- 执行站点 JS 扩大不可信代码面，需更强的网络/资源隔离。
- JS 页面结构漂移更频繁，需配套漂移检测。

### 16.3 分期路径（决策）

以下路径被 2026-08-11 双轨决策取代：不再把浏览器沙箱加入写入型数据源接入助手，也不用于已发布源的任意动态采集。

1. 本计划仅维护静态接口与服务端渲染 HTML 的既有探测能力。
2. Crawl4AI 仅在研究轨开发阶段0及 ECS部署验证阶段0做受限单页读取验证，不开放任意深爬或浏览器点击。
3. 验证通过也不自动形成正式数据源；固定信源接入与通用研究分别按 v1.2 总计划重新审批。
