# 智能信源采集与分析落地执行计划

> 版本：v1.2（研究轨开发阶段0批准稿） 日期：2026-08-11
> 关联文档：《数据源智能接入与实时查询实施计划》《风险监控维度扩展规划》《阿里云部署实施清单》《智能信源采集与分析落地执行计划评估》
> v1.1 → v1.2 变更：落实条件审批意见；研究任务改为持久化异步 Worker；补齐工作认领、结论—引用关系、旁路执行契约、RBAC、留存与查询最小化；仅批准研究轨开发阶段0技术 Spike。

> 术语统一：**研究轨开发阶段0**指本地代码、测试、受控 Agent 和本地镜像研发；**ECS部署验证阶段0**指在 ECS 上叠加旁路容器、真实单任务和 48 小时容量/稳定性验证。两者是前后衔接的独立门禁，研究轨开发阶段0完成不等于 ECS部署验证阶段0完成。

## 1. 目标

在产品定位调整为"风险报告仅供管理层及业务人员参考、不作为重大决策核心依据"的前提下，建设双轨信息采集与分析能力：

**监控轨（确定性，保留并精简）**：天气、自然灾害等即时类信源维持高频抓取，走现有 `raw_signals` → 一次 AI 解析 → P1-P4 风险提醒链路，零改动。

**研究轨（Agent，新增）**：内置研究 Agent 在不限于预置信源的范围自主搜集风险与行业信息，经来源分级与交叉验证后输出带逐条引用的研究报告；支持手动即时任务与月报试点（默认最多 100 家，保留全量开关）；研究结论仅在人工确认后可转为正式风险信号。

本计划交付的闭环：

1. 新增 RSS/网页类信源零编码，仅改配置。
2. 研究 Agent 自主完成"搜索 → 读取 → 验证 → 报告"，每条关键结论绑定可回验的引用。
3. 全部 LLM 调用复用现有 Provider 抽象、预算与审计；单轨单次解析，无重复调用。
4. 研究轨开发阶段0的本地基线最终要由 ECS部署验证阶段0在现有 2 核 4G ECS复核，浏览器并发固定为 1；是否长期共存以整机峰值保留 25-30% 内存余量为准，未达标则升级 4 核 8G 或拆分抓取服务。
5. 采集与抓取行为满足频率限速、robots 遵从与来源合规要求，全程可审计。

## 2. 产品定位调整

### 2.1 定位变化

- 放弃继续投入专用数据源导入 Agent 的深度开发；既有声明式适配器能力保留维护。
- 平台输出分两类：**风险提醒**（监控轨，沿用现有分级与呈现）与**研究报告**（研究轨，明确标注"仅供参考"）。
- 研究报告不进入自动评分链，不承担 P1-P4 分级；转为正式风险信号必须经管理员确认。

### 2.2 产品基线同步状态

`PRODUCT.md` 已于 2026-08-11 同步双轨定位、研究报告参考边界和人工转正原则；`CONTEXT.md` 已同步研究任务、来源、引用、结论、报告和结论转正等领域术语。

## 3. 范围与取舍

### 3.1 本计划支持

- 原生 RSS/Atom 直连；RSSHub 路由；Crawl4AI 网页读取。
- 即时类信源高频抓取（≥30 分钟可配）；非即时类按日/按周；手动即时触发。
- 研究 Agent：自主生成查询、搜索发现、网页读取、来源分级、交叉验证、事实/推断/预测分段、逐条引用报告。
- 研究任务类型：手动即时研究、全供应商月度风险研究与组合月报；日报和周报不进入本期自动执行范围。
- 研究结论经人工确认转为 `raw_signals`。

### 3.2 本计划不支持

- 需要登录态、验证码、OAuth 交互的信源与站点。
- 强反爬平台（微信、微博等）的非官方抓取；仅使用经逐项核准的 RSSHub 路由或授权商业数据服务。
- 使用代理轮换绕过 WAF 或封禁（项目既定边界）；目标站点被拒时只能停源或改用授权渠道。
- PDF/OCR、音视频解析（列入后续路线）。
- n8n 或任何第二套编排/调度系统（维持评估结论：当前 Scheduler 已具备每源独立 cron，n8n 降为阶段三候选，不作为本期承诺）。

## 4. 总体架构

```
监控轨（确定性，高频）                研究轨（Agent，低频）
天气/灾害/官方公告                     研究任务（手动/月报试点或全量）
  ↓ 直连 RSS / RSSHub 路由               ↓ Agent 生成查询
raw_signals                          搜索服务（SEARCH_PROVIDER）
  ↓ 指纹去重                             ↓ 候选 URL
相关性预过滤                            Crawl4AI 网页读取
  ↓ 现有一次 LLM 解析                    ↓ 来源分级 + 交叉验证
风险事件 → 关联 → P1-P4 提醒      research_reports + claims + citations
        ↑                                ↓ 人工确认门
        └──────── 转为风险信号 ──────────┘

共享底座：Crawl4AI 容器（浏览器并发=1）、RSSHub 容器、
          source_host_access 域名治理、AI Provider 抽象、PostgreSQL 任务队列
```

组件职责与资源预算：

| 组件 | 职责 | 部署形态 | 内存预算 |
|---|---|---|---|
| RSSHub | 监控轨无 RSS 信源的路由化接入 | 旁路容器，固定版本+摘要 | ≤512MB |
| Crawl4AI | 双轨共享网页读取 | 旁路容器，固定版本+摘要，浏览器并发=1 | ≤1536MB |
| 研究 Agent | 查询生成、工具编排、报告产出 | `research-worker` 独立进程（同一应用镜像） | 低并发单 Worker |
| FastAPI | 创建任务、查询状态、人工确认 | 现有 Web 进程；请求不执行长研究任务 | 复用 |
| Scheduler | 创建到期任务、触发监控采集，不执行长研究流程 | 现有组件 | 复用 |

## 5. 数据模型

### 5.1 数据源字段调整

不改动 `source_type` 的业务语义（weather、policy、industry、sanctions 等），新增传输方式字段：

- `connector_kind`：`builtin`、`declarative_json`、`rss`、`rsshub`、`crawl4ai`。
- `adapter_config`：按 `connector_kind` 分别做 Schema 校验的连接器参数。
- 该变更伴随约束与索引一并迁移；不作为孤立的文本值扩充。

### 5.2 研究轨新增表

- `research_tasks`：任务定义与执行状态（manual/monthly，保留 daily/weekly 历史兼容；主题；供应商范围；状态；创建者；预算快照；认领租约；重试与取消信息）。
- `research_sources`：任务实际检索到的来源（URL、最终目标域名、来源分级、获取时间、HTTP 状态）。
- `research_citations`：引用记录（URL、最终 URL、标题、发布时间、访问时间、规范化正文哈希、正文摘录、摘录起止位置、回验状态）。
- `research_reports`：报告（正文、事实/推断/预测分段标记、模型版本、生成状态、关联任务、人工确认状态）。
- `research_claims`：报告中的原子结论（fact/inference/forecast；结论文本；验证状态；置信度；转正状态）。
- `research_claim_citations`：结论与引用的多对多关系；转正操作以单条结论为单位。

## 6. 采集治理（覆盖旁路容器的真实出网请求）

针对评估 P0-2，治理必须落在最终目标域名上，而非内部服务地址：

1. **平台前置校验**：创建采集/研究任务时验证最终目标 URL；仅 HTTPS；解析后拒绝 loopback、私网、链路本地、保留地址、云元数据地址；重定向逐跳校验。
2. **RSSHub 白名单**：只允许登记过的路由模板，禁止任意路由参数；每路由记录原网站、目标域名、维护状态、数据条款、robots 要求、是否允许自动采集与正文留存。
3. **Crawl4AI 策略配置**：种子 URL、允许域名、最大深度与页面数、链接包含/排除规则、内容大小上限、JS 开关、超时与任务预算——"免选择器"不等于免抓取策略。
4. **旁路执行契约**：平台先为声明的最终目标域名申请 `source_host_access` 租约，再调用旁路服务；旁路返回最终 URL、重定向链与 HTTP 状态，平台据此完成租约并写入冷却。研究轨开发阶段0和 ECS部署验证阶段0的 Crawl4AI 只允许单页读取，不开放任意深度抓取。
5. **出网防火墙**：容器网络层禁止访问云元数据、私网、回环与宿主机管理端口。
6. **旁路自治保护**：RSSHub/Crawl4AI 仍须配置允许域名、单浏览器、页数上限、最小间隔和超时；它们不得直接读写平台数据库，也不能替代平台租约。RSSHub 路由必须绑定登记的真实上游域名。
7. **熔断与冷却**：复用 `source_host_access` 的 403/WAF 熔断、429 冷却、网络错误退避；被 robots/403/429 拒绝的目标不得继续重试。重定向跨域时必须释放原租约并为新域名重新申请租约。
8. **镜像固定**：生产镜像固定版本号与摘要，记录 Node/Python/浏览器版本；升级前回归验收并留回滚记录；抓取服务不对公网暴露，不接受任意脚本或任意浏览器配置。

## 7. 研究 Agent 设计

### 7.1 工具集与执行方式

- `web_search`：调用 SEARCH_PROVIDER 返回候选 URL。
- `read_page`：经 Crawl4AI 读取并提取正文（受第 6 节治理约束）。
- `register_citation`：登记引用并触发摘录回验。
- `lookup_supplier`：查询供应商主数据，限定关联候选范围。
- `submit_report`：提交结构化报告草稿。

Agent 不生成或执行任意代码；工具白名单之外的请求一律拒绝。

研究任务不得在 FastAPI 请求生命周期中同步完成。`POST` 创建任务后返回 `202 + task_id`，由单独的 `research-worker` 从 PostgreSQL 认领并执行；前端查询状态或取消任务。现有 `agent/engine.py` 只复用模型 Provider、工具描述、参数脱敏等基础能力，不直接复用面向短对话的 6 步/短上下文循环。

任务状态固定为 `pending`、`running`、`succeeded`、`failed`、`cancelled`、`budget_exhausted`。Worker 使用 `FOR UPDATE SKIP LOCKED` 或等价租约认领任务，保存步骤、累计搜索次数、Token、金额和中间结果；进程重启后可恢复，租约过期后才能重新认领。

### 7.2 可信度机制（非纯模型自判）

- **来源分级表**：官方机构 > 授权商业平台 > 主流媒体 > 行业自媒体；分级落 `research_sources`。
- **交叉验证**：非官方关键事实须至少两个不同所有者、不同原始出处的来源支持；同一通讯稿的转载归为同一来源族。官方一手公告可单源成立；单一低分级来源的结论标记为"待核实"。
- **LLM 判断仅作辅助**，模型输出的可信度评估不得单独作为结论依据。

### 7.3 引用绑定与回验

- 每条关键结论作为独立 `research_claims` 记录，并通过 `research_claim_citations` 至少绑定一条引用。
- 落库前系统回验：摘录文本确实存在于对应 URL 的已抓取正文中。
- 引用缺失、回验失败或正文不支持结论时，报告降级（去除该结论）或整体标记失败。

### 7.4 报告结构

- 事实、推断、预测三段强制分离；每段结论逐条带引用编号。
- 报告头部固定声明："本报告由 AI 生成，仅供内部参考，不作为重大决策依据"。
- 保留模型版本、任务配置快照与生成状态，支持复现与追责。

### 7.5 转正闸门

- `risk_admin` 或 `platform_admin` 在阅读报告时可对某条结论执行"转为风险信号"；系统按 `research_claim_id` 幂等写入 `raw_signals` 并携带引用上下文，随后走现有一次 LLM 解析与评分链。
- 未确认的研究结论永远不进入评分链。

### 7.6 权限边界

- `viewer`：查看已发布且在其业务范围内的研究报告。
- `risk_analyst`：创建和取消自己的手动研究任务，查看其有权访问的报告。
- `risk_admin`：配置日/周任务、查看业务研究审计、确认单条结论转为风险信号。
- `platform_admin`：配置搜索 Provider、全局预算、研究 Worker 与全局止损开关。

研究任务必须绑定创建者；任何角色均不得越权读取他人的受限任务或供应商范围。

## 8. LLM 与成本控制

1. **单次解析原则**：监控轨沿用现有"一次 LLM 解析"（分类、主体/地点/产品提取、摘要、证据句、置信度、Schema 校验重试），本计划不新增前置分析调用。LLM 不参与指纹生成与确定性去重。
2. **Provider 复用**：全部模型调用走现有 `AI_PROVIDER`/`AI_BASE_URL`/`AI_MODEL`/`AI_API_KEY` 抽象；不新增平行的厂商专用配置。模型 ID、上下文上限与计费以当日控制台核实为准，不写死在文档中。
3. **搜索服务抽象**：新增 `SEARCH_PROVIDER`/`SEARCH_API_KEY`，Bocha 与 Tavily 仅作为研究轨开发阶段0候选；主备选择取决于 ECS部署验证阶段0的实测、合同条款、价格和结果质量。主备切换必须经过熔断状态，不得为同一查询无限双发或重复计费。
4. **预算硬上限**：单信源每日条目上限；单正文最大字符数；单研究任务最大步骤、搜索次数、网页数、Token 与金额；全局每日 Token 与金额上限；每日抓取页数与并发上限；LLM 前确定性相关性预过滤。
5. **止损开关**：超预算自动停止新增分析；管理员可立即暂停 Scheduler 与手动研究任务。
6. **成本公式**：月成本 = Σ(日任务数 × 单任务搜索次数 × 搜索单价) + Σ(日处理条目 × 平均输入输出 Token × 模型单价)。以按日频率、每日 1 个研究任务（约 20 次搜索、50 条正文）粗估：搜索约数十元/月，LLM 约数十元/月；开发前以当日价格验证，不作为承诺值。
7. **查询最小化**：搜索查询只允许包含公开企业名称和公开研究主题，禁止发送内部供应商编码、采购金额、内部备注、凭据或完整供应商清单；搜索日志按同一规则脱敏。

## 9. 部署方案与资源核算（按整机）

- 环境：阿里云 ECS 2 核 4G，现有基础服务（PostgreSQL/PostGIS、FastAPI、Scheduler、Nginx、Docker/OS）实测基线先行记录。
- 新增常驻进程为 RSSHub（≤512MB）、Crawl4AI（≤1536MB，浏览器并发=1）和单实例 `research-worker`（复用应用镜像，不含本地推理），经 `compose.sources.yaml` 并入内部网络，不暴露公网端口。
- 验收口径为整机：峰值时保留 ≥25-30% 可用内存；连续 48 小时 OOM 次数为 0；任务积压可熔断；重启后任务可恢复且不重复调用模型。
- 容量边界判断：2C4G 仅承诺低并发 POC；RSSHub + Crawl4AI 与现有平台长期共存建议升 4 核 8G；未来叠加多用户并发研究时拆分实例。

## 10. 实施阶段

### 研究轨开发阶段0：技术 Spike 与本地研发基线（预估 2-3 个工作日）

本节的代码、测试和本地镜像属于研究轨开发阶段0；RSSHub/Crawl4AI 在 ECS 上的真实叠加、Provider 实测、48 小时浸泡和整机容量属于独立的 ECS部署验证阶段0。

1. 为 ECS部署验证阶段0部署固定版本的 RSSHub 与 Crawl4AI 旁路容器。
2. 在 ECS部署验证阶段0为三个连接器各验一个样本：原生 RSS、RSSHub 路由、Crawl4AI 页面。
3. 在 ECS部署验证阶段0实测：整机峰值内存/CPU；单页面正文长度与 Token 数；robots/SSRF/重定向/403/429/超大响应行为；容器重启与任务恢复；域名治理是否落在最终目标主机。
4. ECS部署验证阶段0搜索服务实测：验证 Bocha/Tavily 可达性、单次延迟与结果质量抽样。
5. 研究轨开发阶段0提供可运行的手动研究闭环；ECS部署验证阶段0在获批的 ECS 试点上跑通“某供应商主体近 30 天风险动态”并生成带引用报告。
6. 定义并验证旁路执行契约、PostgreSQL 任务认领、任务取消与重启恢复；研究轨开发阶段0的本地任务串行执行。
7. 为 ECS部署验证阶段0固定第三方镜像版本与摘要，保存依赖/SBOM和漏洞扫描结果。

**研究轨开发阶段0验收**：代码、测试、本地镜像和受控研究闭环有记录；ECS旁路、真实 Provider、48 小时连续运行和整机容量由 ECS部署验证阶段0另行验收。2-3 个工作日指主动研发投入，不包含 ECS部署验证阶段0的观察窗口。

### 阶段 1：监控轨连接器扩展（预估 3-4 个工作日）

1. `connector_kind` 字段与配置 Schema 迁移。
2. 原生 RSS、RSSHub、Crawl4AI 三个连接器；复用现有指纹去重、`raw_signals`、一次 LLM 解析与 Scheduler。
3. 采集运行记录与来源健康状态。
4. 即时类信源频率配置收敛（≥30 分钟），非即时类调整为按日/按周。
5. 待处理信号使用数据库工作认领或全局串行锁，禁止多个采集任务重复选择并分析同一信号。

**阶段 1 验收**：同一原文重复采集不产生重复 `raw_signals`、不重复调用 LLM；新增信源零编码。

### 阶段 2：研究轨（预估 5-8 个工作日）

1. 研究轨六表迁移与 `SEARCH_PROVIDER` 抽象。
2. Agent 工具集（7.1）、`research-worker` 与任务编排（手动/月报试点或全量）。
3. 来源分级、交叉验证、引用回验、三段式报告。
4. 报告阅读界面与"转为风险信号"人工确认门。
5. 预算硬上限与止损开关接入。

**阶段 2 验收**：报告每条关键结论有可回验引用；事实/推断/预测分段正确；超预算自动停止；未确认结论不进入评分链。

### 阶段 3：n8n 再评估（按需）

仅当出现复杂跨系统 DAG、审批通知编排或非技术人员维护工作流的真实需求时再评估引入；当前不承诺。

## 11. 验收标准（总体）

1. 新增 RSS/网页类信源零编码，仅改配置。
2. 同一原文重复采集不产生重复 `raw_signals`，不重复调用 LLM。
3. 旁路容器发出的真实目标域名可被平台记录、限速与冷却。
4. 被 robots、403、429 或 WAF 拒绝的目标站点不被继续重试。
5. 抓取服务不接受任意脚本、任意私网 URL 或任意浏览器会话。
6. 研究报告每条关键结论有对应引用；引用不存在或正文不支持结论时报告降级或失败。
7. 报告明确区分事实、推断与预测，并固定"仅供参考"声明。
8. 单日 Token、金额、抓取页数与并发均有硬上限；超预算自动停止，管理员可立即暂停。
9. 2C4G POC 连续 48 小时零 OOM、零任务重复、零未处理积压；峰值保留 ≥25-30% 内存。
10. 镜像与模型版本固定，升级有回滚记录。
11. 研究任务异步执行，FastAPI 创建任务后立即返回；任务支持取消、重启恢复、租约过期重领且不重复计费。
12. 单条研究结论与引用关系结构化保存，同一结论最多转为一个正式风险信号。

## 12. 安全与合规

1. 信源与研究来源仅覆盖官方机构、授权商业平台与公开信息渠道；每条来源登记来源性质与授权说明。
2. RSSHub 路由不等于官方授权；逐路由核查数据条款与 robots 要求。
3. 禁止代理轮换绕过封禁；被拒目标只能停源或改用授权商业数据服务。
4. 凭据仅存于环境变量/外部密钥引用；不进入信源配置、日志与 LLM 上下文。
5. 搜索查询执行数据最小化；不得向搜索服务发送内部供应商编码、采购金额、内部备注或完整供应商清单。
6. 采集与研究内容仅用于内部参考，不二次分发；监控轨沿用 `raw_signals` 策略，研究轨分别定义搜索缓存、抓取正文、引用、报告、失败任务与工具日志的保留期限。
7. 研究报告固定"AI 生成、仅供参考"声明；对外分享报告需人工审核。
8. 信源与任务的新增、启停、取消、采集执行、LLM 调用、报告发布和结论转正（脱敏）全部落审计日志。

## 13. 研究轨开发阶段0启动准备事项

1. 选择研究轨开发阶段0排期、2 个试点固定信源和 1 个公开研究主题；ECS部署验证阶段0另行确认上线窗口。
2. 实测 Bocha、Tavily 等候选服务后确认搜索 Provider 主备及月度预算上限，不预设未经验证的固定组合。
3. 准备固定版本镜像、SBOM 与漏洞扫描结果。
4. 明确 48 小时整机容量实测记录格式，并接受余量不足时升级 4 核 8G 或拆分服务的止损预案。

## 14. 风险与回退

| 风险 | 缓解 | 回退 |
|---|---|---|
| 搜索服务不可达或失效 | 主备双 Provider + 可达性实测 | 降级为白名单种子域名站内检索（RSSHub + Crawl4AI） |
| RSSHub 路由失效 | 周巡检 + 失败告警 | 该源改 crawl4ai 连接器或停用 |
| 目标站封禁 ECS IP | 限速 + 真实 UA + 熔断冷却 | 停源或改用授权商业数据服务（禁止代理轮换） |
| LLM 输出不稳定 | Schema 校验 + 重试 + 引用回验 | 报告降级为仅事实摘录，标注未分析 |
| 研究 Agent 结论失真 | 来源分级 + 双源交叉验证 + 三段分离 | 报告仅供阅读，关闭转正闸门 |
| 2C4G 资源不足 | 浏览器并发=1 + 容器限额 + 48 小时实测 | 仅保留 RSS 直连与 RSSHub，Crawl4AI 按需启停；或升 4 核 8G |

## 15. 审批状态与开工门

当前审批结论：**架构方向通过；仅批准研究轨开发阶段0技术 Spike；ECS部署验证阶段0需独立执行和验收。**

阶段 1 开工前必须同时满足：

1. 研究轨开发阶段0完成，且 ECS部署验证阶段0的容量、网络治理、搜索质量和 48 小时浸泡记录通过；
2. 旁路执行契约能够证明最终目标域名被正确租约、限速和冷却；
3. PostgreSQL 任务认领、取消、重启恢复和幂等验证通过；
4. 已决定维持 2 核 4G、升级 4 核 8G 或拆分抓取服务；
5. 固定镜像、搜索 Provider、预算上限和试点来源获得批准。

阶段 2 需在阶段 1 验收后单独批准。研究轨开发阶段0和 ECS部署验证阶段0均不授权正式发布、批量采集、生产凭据录入、对外开放抓取服务或启用自动研究任务。

## 16. 研究轨开发阶段0开工记录（2026-08-11）

本轮开始研究轨开发阶段0的第一垂直切片，范围限定为研究任务持久化与任务治理，不接入真实搜索 Provider、RSSHub、Crawl4AI 或生产凭据。

### 16.1 本轮修改范围

- `backend/app/research/`：研究任务、研究来源、研究引用、原子结论的领域模型与任务服务。
- `backend/alembic/versions/0022_research_foundation.py`：新增研究任务基础迁移，包含创建者、状态、租约、取消和幂等字段。
- `backend/app/research/router.py`、`schemas.py`、`backend/app/main.py`：提供创建任务、查询状态、取消任务和内部受控认领能力；长任务不在 HTTP 请求中执行。
- `backend/tests/test_research_tasks.py`：覆盖任务创建、创建者隔离、租约认领、租约过期恢复、取消和重复认领边界。

### 16.2 本轮明确不做

- 不调用 Bocha、Tavily 或其他真实搜索服务，不记录或录入任何 API Key。
- 不部署 RSSHub/Crawl4AI，不开放公网端口，不执行真实外部网页抓取。
- 不把研究结论写入 `raw_signals`，不触发监控评分，不新增自动日/周任务。
- 不修改现有风险评分、数据源接入助手和历史 MVP 验收事实。

### 16.3 本轮验收门

1. `POST` 创建任务返回 `202` 和 `task_id`，不在请求生命周期执行研究。
2. 任务只能由创建者或授权管理员读取；取消操作幂等且不能取消已完成任务。
3. Worker 认领使用数据库原子条件，租约过期后可恢复认领，同一任务不会同时被两个 Worker 有效持有。
4. 迁移可升级/降级，单元测试覆盖状态转移和权限边界。
5. 通过后再编写搜索 Provider 与单页读取 Spike，不把基础任务表误报为完整研究能力。

### 16.4 第一垂直切片状态（2026-08-11）

- 已完成 `0022` 迁移：研究任务、研究来源、研究引用、研究结论和结论—引用关系表已建立。
- 已完成任务 API：风险分析员及以上角色可创建、查询和取消自己的任务；管理员可查看业务范围内任务；创建接口返回 `202`，不执行长研究。
- 已完成 Worker 治理服务：数据库行锁认领、租约过期恢复、取消请求优先级、错误 Worker 不可完成和幂等创建。
- 验证证据：新增研究任务测试 4/4 通过；容器内 Ruff 通过；mypy 71 个源文件通过；隔离数据库 `0021→0022` 升级、`0022→0021→0022` 回滚演练通过。
- 隔离数据库后端回归为 205 项通过；另有 1 项既有认证测试因测试顺序共享用户数据而失败，单独在全新数据库运行该用例通过，不属于本切片引入的失败。

第一垂直切片仍不代表研究能力已交付：尚未接入搜索 Provider、RSSHub、Crawl4AI、研究 Worker 常驻进程或真实外部网页。

### 16.5 第二切片实施范围：受控单页读取（当前轮次）

- `backend/app/research/web.py`：实现仅限公网 HTTPS 的单页读取，逐跳校验重定向，记录请求 URL、最终 URL、重定向链、HTTP 状态、内容类型和截断正文摘要。
- 复用 `validate_public_https_url` 的公网地址校验和 `source_host_access` 的域名租约、限速、冷却、WAF/429 判断；MockTransport 测试不写入域名治理状态。
- 单页读取设置固定最大响应字节数、最大重定向次数和摘要字符数；不执行任意脚本、不登录、不点击、不遍历站内链接。
- `backend/tests/test_research_web.py`：覆盖重定向链、最终 URL、私网/非 HTTPS 拒绝、响应大小限制、403/429 失败和 HTML 摘要脱除脚本。

本切片仍不接入真实 Provider、Crawl4AI 或研究 Worker；真实站点现场验证需另行取得 ECS部署验证阶段0试点信源批准。

### 16.6 第二切片验收状态（2026-08-11）

- 已完成受控单页读取基础：仅允许公网 HTTPS，重定向逐跳校验，固定响应大小、重定向次数和摘要字符上限；不执行 JavaScript、不登录、不点击、不遍历站内链接。
- 已复用现有域名级租约、限速、冷却及 403/429/WAF 判断；同一页面的同域重定向复用租约，跨域重定向释放旧租约后重新申请；使用 MockTransport 的协议测试不写入域名治理状态。
- 网页读取测试 `backend/tests/test_research_web.py`：9/9 通过，覆盖重定向链、最终 URL、重定向目标逐跳校验、可见 HTML 摘要、私网/非 HTTPS、响应大小、403/429、缺少 Location 和重定向上限。
- 容器内 Ruff：通过；容器内 mypy：72 个源文件通过。
- 已重建 `supplierriskmonitoring-app` 镜像，并在重建后的镜像内复跑网页测试 9/9 通过；本次未重启现有 app/scheduler 容器。

第二切片只交付受控单页读取基础，不代表完整研究能力已交付；尚未接入搜索 Provider、RSSHub、Crawl4AI、研究 Worker 常驻进程、自动调度或真实外部网页采集。

### 16.7 第三切片实施范围：搜索 Provider 协议与预算闸门（当前轮次）

- `backend/app/research/search.py`：定义 `SearchProvider` 协议、统一搜索候选结构、查询规范化、HTTPS 结果过滤、去重、每查询/每任务结果上限和不可自动双发的预算闸门。
- `backend/tests/test_research_search.py`：使用确定性 `FakeSearchProvider` 验证查询边界、结果清洗、HTTPS/凭据/内部域名拒绝、查询次数和总结果预算。
- 本切片不增加搜索服务凭据，不调用 Bocha/Tavily，不实现商业 Provider 适配，不执行主备切换，不写入研究来源表。

### 16.8 第三切片验收状态（2026-08-11）

- 已完成搜索 Provider 最小协议：`SearchProvider`、标准候选结果、查询规范化和不安全 HTTPS 结果过滤；搜索结果必须继续经过受控单页读取，不直接视为证据。
- 已完成任务级确定性预算闸门：每次查询先扣减预算，限制单次最多 20 条、任务总查询数和总结果数；Provider 失败不自动双发、不自动主备切换，避免重复计费。
- `FakeSearchProvider` 仅用于协议测试，不执行网络请求、不访问数据库。
- 搜索测试与网页读取测试合计 20/20 通过；研究轨 Ruff 通过；mypy 73 个源文件通过。
- 已重建 `supplierriskmonitoring-app` 镜像，并在最终镜像内复跑两组测试 20/20 通过；本次仍未重启现有 app/scheduler 容器。

第三切片仍不代表搜索服务已接通，也不代表研究 Agent 已具备端到端执行能力；商业 Provider、主备策略、搜索质量和 ECS 网络实测仍需单独批准后实施。

### 16.9 第四切片实施范围：来源快照与引用回验基础（当前轮次）

- `backend/app/research/citations.py`：将单页读取结果转换为 `research_sources` 来源快照，保存最终 URL、重定向链、状态、规范化正文摘要和摘要哈希；提供引用片段规范化与正文命中回验。
- `backend/tests/test_research_citations.py`：覆盖空白规范化、哈希稳定性、来源元数据、引用命中/未命中和 ID/长度边界。
- 哈希范围明确为 `normalized_excerpt`；在单页读取器未保留完整正文时，不将摘要哈希宣称为完整原文审计哈希。
- 本切片不生成报告、不调用 LLM、不写入正式风险信号；未验证引用只能标记为 `verified=false`，不得直接支撑结论转正。

### 16.10 第四切片验收状态（2026-08-11）

- 已完成来源快照构造：保存最终 URL、原始请求 URL、重定向链、HTTP 状态、内容类型、规范化正文摘要和 `normalized_excerpt` 哈希范围。
- 已完成引用回验：引用片段与规范化正文摘要匹配时标记 `verified=true`，未命中时保留记录但标记 `verified=false`，不自动提升为可信结论。
- 引用回验测试 7/7 通过；搜索、单页读取、引用回验三组测试合计 27/27 通过；Ruff 通过；mypy 74 个源文件通过。
- 已重建 `supplierriskmonitoring-app` 镜像，并在最终镜像内复跑三组测试 27/27 通过；本次未重启现有 app/scheduler 容器。

第四切片只交付来源快照与引用回验基础，不代表完整报告生成、引用回验服务编排或人工确认流程已交付。

### 16.11 第五切片实施范围：结构化报告草稿与人工确认闸门（当前轮次）

- `backend/app/research/reporting.py`：定义事实/推断/预测三段报告草稿契约，要求每条结论绑定存在且已回验的引用，并固定“AI 生成，仅供参考”免责声明。
- 提供 `can_promote_claim` 纯函数：只有报告结构化校验通过且显式传入人工确认，才返回允许转正；本切片不执行 `raw_signals` 写入。
- `backend/tests/test_research_reporting.py`：覆盖合法报告、免责声明、未验证引用、缺失引用、分段类型、重复 ID、HTTPS URL 和人工确认门。
- 本切片不调用 LLM、不生成报告正文、不提供报告 API、不启动 Worker。

### 16.12 第五切片验收状态（2026-08-11）

- 已完成结构化报告草稿契约：事实、推断、预测三段分开保存；每条结论必须绑定存在且已回验的引用；报告必须包含“AI 生成，仅供参考”免责声明。
- 已完成人工确认闸门：未显式人工确认时不可返回允许转正；函数只返回判定，不写入 `raw_signals`，不触发评分链。
- 报告契约测试 6/6 通过；搜索、单页读取、引用回验、报告契约四组测试合计 33/33 通过；Ruff 通过；mypy 75 个源文件通过。
- 已重建 `supplierriskmonitoring-app` 镜像，并在最终镜像内复跑四组测试 33/33 通过；本次未重启现有 app/scheduler 容器。

第五切片只交付结构化报告和人工确认边界，不代表 LLM 报告生成、报告 API、研究 Worker 或结论转正流程已经上线。

### 16.13 第六切片实施范围：报告草稿持久化与受限查询（当前轮次）

- `backend/app/research/models.py`、`backend/alembic/versions/0023_research_reports.py`：新增报告草稿表，保存结构化草稿快照、模型版本、草稿状态和人工审核状态；任务删除时级联删除草稿。
- `backend/app/research/service.py`、`schemas.py`、`router.py`：新增报告草稿保存和按任务查询接口，沿用任务创建者隔离与管理员读取规则；保存前执行报告结构化校验。
- `backend/tests/test_research_reports.py`：覆盖保存/查询、未验证引用拒绝、创建者隔离和 viewer 权限边界。
- 本切片不提供提交发布、审批、结论转正、LLM 调用或 Worker；草稿保存不会修改 `raw_signals`。

### 16.14 第六切片验收状态（2026-08-11）

- 已新增 `research_reports` 表及 `0023` 迁移；开发数据库已从 `0022` 升级到 `0023 (head)`。
- 报告草稿 API 已支持保存和按任务查询；沿用创建者隔离，`risk_admin`/`platform_admin` 可读取业务范围内任务，viewer 无权限。
- 保存前强制执行报告免责声明、三段类型、引用存在性、引用回验和 HTTPS URL 校验；草稿状态为 `draft`，审核状态初始为 `pending`。
- 报告草稿 API 测试 4/4 通过；研究轨开发阶段0研究轨回归 41/41 通过；Ruff 通过；mypy 75 个源文件通过。
- 已重建并替换运行中的 app 容器；app、PostgreSQL 均为 healthy，健康接口返回数据库 `ok`；scheduler 当前未运行。

第六切片仍不提供报告审批、发布、结论转正、LLM 生成或研究 Worker；`research_reports` 只是受控草稿存储。

### 16.15 第七切片实施范围：智能研究前端入口（当前轮次）

- `frontend/src/components/ResearchView.tsx`：新增“研究轨”页面，支持手动创建研究任务、查看任务状态和读取报告草稿；页面固定“仅供参考”与未启用自动研究的边界提示。
- `frontend/src/api.ts`、`frontend/src/types.ts`：新增研究任务、报告草稿类型和 API 调用；只调用现有研究任务/报告查询接口。
- `frontend/src/App.tsx`、`Sidebar.tsx`、`MobileNav.tsx`：按 `research_task_create` 权限显示“智能研究”入口，桌面和窄屏导航一致。
- 本切片不增加自动调度、搜索按钮、LLM 生成、报告审批或风险信号转正入口。

### 16.16 第七切片验收状态（2026-08-11）

- 前端 TypeScript 检查通过；Vitest 4/4 通过；生产构建通过。
- Docker 构建阶段内置前端测试和类型检查通过；app 镜像已重建并替换运行中容器。
- app、PostgreSQL 均为 healthy，健康接口返回数据库 `ok`；scheduler 仍未运行。

第七切片完成后，用户可以从前端创建手动研究任务并查看草稿；由于搜索 Provider 和 Worker 尚未接入，新任务不会自动产生外部抓取或 LLM 费用。

### 16.17 第八切片实施范围：研究任务状态刷新与取消（当前轮次）

- `frontend/src/api.ts`：新增研究任务详情查询和取消任务调用，分别对应 `GET /api/v1/research/tasks/{task_id}` 与 `POST /api/v1/research/tasks/{task_id}/cancel`。
- `frontend/src/components/ResearchView.tsx`：对 `queued`/`running` 任务增加 5 秒轮询；任务进入 `succeeded` 后自动重新读取报告草稿；增加取消任务按钮、取消请求中的禁用状态和“已请求取消”提示。
- 轮询只刷新当前选中的任务，并在页面卸载或切换任务时清理定时器；不新增搜索、LLM、Worker 或自动调度调用。

### 16.18 第八切片验收状态（2026-08-11）

- 前端 TypeScript 检查通过；Vitest 1 个测试文件、4/4 测试通过；生产构建通过。
- Docker 构建阶段内置前端测试、类型检查和生产构建均通过；`supplierriskmonitoring-app` 镜像已重建。
- 已使用 `docker compose up -d --no-deps --force-recreate app` 替换 app 容器；app、PostgreSQL 均为 healthy，`/api/v1/system/health` 返回 `{"status":"ok","database":"ok"}`。
- scheduler 当前未运行；本切片没有产生搜索 Provider、外部网页或 LLM 调用，也没有改变 `raw_signals`。

第八切片完成后，前端具备研究任务的最小状态闭环：创建后可自动观察排队/运行状态，任务完成后刷新草稿，必要时可发起取消请求。研究 Worker、真实搜索 Provider、LLM 报告生成和自动调度仍未启用。

### 16.19 第九切片实施范围：前端研究 API 契约测试（当前轮次）

- `frontend/src/api.test.ts`：补充研究任务列表、任务详情、任务取消、手动创建和报告草稿查询的请求契约测试。
- 测试验证路径、HTTP 方法、JSON 请求体、会话 Cookie 和写请求 CSRF 头；使用本地 `fetch` mock，不访问真实 API、不产生外部费用。
- 本切片不新增产品入口、不调整后端接口、不启用研究 Worker、搜索 Provider、LLM 或 scheduler。

### 16.20 第九切片验收状态（2026-08-11）

- 前端 TypeScript 检查通过；Vitest 1 个测试文件、6/6 测试通过，覆盖研究任务列表/详情/取消/创建及报告查询请求契约。
- 前端生产构建通过；Docker app 构建阶段的测试、类型检查和生产构建均通过；仍存在既有单个 JS chunk 超过 500 kB 的非阻断警告。
- 本切片仅新增测试，不改变运行时 bundle，因此未替换运行中的 app 容器；现有 app、PostgreSQL 仍为 healthy，scheduler 保持未运行。

第九切片完成后，研究前端 API 的关键路径具备回归保护，后续接入真实 Worker 或 Provider 时，接口方法、路径和 CSRF 约定不会因前端改动静默漂移。

### 16.21 第十切片实施范围：研究 Worker 单次执行骨架（当前轮次）

- `backend/app/research/worker.py`：新增仅执行一次的 Worker 编排函数，复用任务租约认领和完成接口；执行器由调用方注入，不内置网络、搜索或模型调用。
- `backend/tests/test_research_worker.py`：覆盖无任务、执行成功、执行异常失败和执行期间收到取消请求等状态转换。
- 本切片不加入常驻 Worker 进程、不修改 compose、不启用 scheduler，不连接真实搜索 Provider、网页抓取服务或 LLM。

### 16.22 第十切片验收状态（2026-08-11）

- 已新增 `run_once` 单次 Worker 编排：无任务返回空；执行器成功后任务变为 `succeeded`；执行器异常变为 `failed`；执行期间收到取消请求后最终变为 `cancelled`。
- 研究轨回归 `tests/test_research_*.py`：45/45 通过；新增 Worker 测试 4/4 通过；Ruff 通过；mypy 76 个源文件通过。
- 修正测试夹具对研究轨表的外键顺序清理，避免持久化开发数据库中的历史队列任务污染租约测试；不改变生产数据保留策略。
- app 镜像已重建并替换运行容器；app、PostgreSQL 均为 healthy，`/api/v1/system/health` 返回 `{"status":"ok","database":"ok"}`；scheduler 未运行。

第十切片只验证 Worker 的任务编排和状态回写，不代表研究 Worker 已部署或具备真实研究能力；常驻进程、续租心跳、搜索/网页/LLM 执行器仍需后续单独批准。

### 16.22 第十一次切片实施范围：本地 research-worker 生命周期测试（当前轮次）

- 新增 `backend/app/research/runner.py` 与 Compose `research-worker` profile；默认关闭，只有显式启用 `research-local-test` profile 才启动。
- 当前 Worker 只认领 `manual` 且主题以 `[local-worker-test]` 开头的任务，验证 PostgreSQL 租约、审计、取消与完成回写；普通研究任务不会被它消费。
- 本地测试执行器不会搜索、读取网页、调用 RSSHub/Crawl4AI、调用模型或访问付费 API；所有用量字段保持为 0，步骤标记为 `local_lifecycle_test_completed`。
- 后续真实执行器必须单独接入受控单页读取、预算预留、续租心跳与结构化报告生成，并在首次外部调用前取得单独批准。

### 16.23 第十二次切片实施范围：受控单页读取执行器（当前轮次）

- 手动任务新增 `source_urls`，每个地址由创建者显式提交；仅接受绝对 HTTPS URL，禁止 URL 内嵌凭据，最多 5 条。Worker 不会从主题文本、搜索结果或网页链接推导新的访问目标。
- `controlled_single_page` Worker 只认领含 `source_urls` 的手动任务；每页请求前预留一次结果用量，并调用既有公网 HTTPS、DNS、重定向、响应大小、域名租约、限速和冷却控制。
- 每个成功响应保存最终 URL、重定向链、响应摘要哈希与摘要文本；摘要非空时保存首段可回验引用。该切片不执行 JavaScript、不跟随页面链接、不调用搜索 Provider、RSSHub、Crawl4AI 或模型，也不生成报告草稿或风险信号。
- 本地真实读取必须显式设置 `RESEARCH_WORKER_ENABLED=true`、`RESEARCH_WORKER_MODE=controlled_single_page` 并以 `research-local-test` profile 启动；首次试验只允许一项手动任务和最多一个批准来源。

### 16.24 第十三次切片实施范围：显式开始受控读取（当前轮次）

- 创建任务不会自动触发外部访问；用户必须在任务详情点击“开始受控读取”。该操作写入 `execution_requested_at` 和脱敏审计事件，Worker 只认领已显式请求执行的任务。

### 16.25 第十四次切片实施范围：按研究主题自动发现信源（当前轮次）

- 前端只要求填写研究主题，不再要求用户提供 URL；“开始研究”仍作为搜索额度和外部网页访问的显式开关。
- `research-worker` 复用现有 `SEARCH_PROVIDER`、任务预算闸门和公网 HTTPS 单页读取器，按任务主题执行一次搜索；候选数量由任务剩余 `max_results`、`max_pages` 与 Provider 单次上限共同约束，并逐页保存来源快照与可回验引用，不另设硬编码数量。
- Provider 未配置、搜索无结果或候选页面读取失败时任务明确失败并记录脱敏错误；不自动切换第二 Provider、不调用 LLM、不生成报告正文、不写入风险评分链。
- 实现和自动化测试使用 Mock/Fake Provider，不消耗真实搜索额度；本地真实调用只在用户点击“开始研究”后发生。
- 没有来源 URL 的任务仍可保存为待补充主题，但开始按钮保持禁用并说明原因；已请求的任务显示“等待 Worker”，避免把本地 Worker 未启动误显示为正在读取。
- 开始操作只授权该任务既有的 `source_urls`，不扩大 URL 数量、搜索范围、模型权限或风险信号写入范围。

### 16.26 第十五次切片实施范围：基于已回验来源生成研究报告草稿（当前轮次）

- 仅在 `topic_source_discovery` 已成功保存至少一个来源和已回验引用后，Worker 才调用一次既有 AI Provider 生成 `ResearchReportDraft`；创建任务与点击“开始研究”的授权边界不变，不新增自动调度。
- 模型输入只包含任务主题、已保存来源的受限摘要和平台分配的引用编号；模型不得提供 URL、抓取目标或浏览器配置。输出中的结论只能引用已提供编号，平台以现有 `validate_report_draft` 复核并以平台侧引用数据覆盖模型回传的引用正文。
- 任一报告结构、引用编号或回验失败均使任务失败且不写 `research_reports`；报告通过后仅保存为 `draft/pending`，固定“AI 生成，仅供参考”声明，不写 `raw_signals`，也不自动转正。
- 一次报告生成最多调用一个已配置 Provider；实际输入/输出 Token 仅记录 Provider 返回的 usage，Provider 未返回 usage 时记为 `0`，不伪造计费金额。开发与回归仅使用 Fake Provider，不发出真实模型请求。

### 16.23 第十一切片实施范围：研究任务租约续租边界（当前轮次）

- `backend/app/research/service.py`：新增当前 Worker 租约续租函数；只允许仍处于有效租约内的同一 Worker 延长租约，过期租约和错误 Worker 均拒绝。
- `backend/tests/test_research_leases.py`：覆盖有效续租、错误 Worker 拒绝、租约过期后重新认领等边界。
- 本切片不启动常驻 Worker、不加入后台心跳线程、不接入搜索/网页/LLM 执行器，也不修改 compose。

### 16.24 第十一切片验收状态（2026-08-11）

- 已新增 `renew_task_lease`：有效租约内仅当前 Worker 可续租；错误 Worker 和已过期 Worker 返回空，不得复活旧租约。
- 租约续租测试 3/3 通过；研究轨全量回归 48/48 通过（包含任务、Worker、租约、网页、搜索、引用和报告测试）。
- Ruff 通过；mypy 76 个源文件通过；app 镜像已重建并替换运行容器。
- app、PostgreSQL 均为 healthy，`/api/v1/system/health` 返回 `{"status":"ok","database":"ok"}`；scheduler 保持未运行。

第十一切片只提供可被后续执行器调用的租约续租边界，不会自动启动 Worker 或产生任何搜索、网页抓取和 LLM 调用。

### 16.25 第十二切片实施范围：研究任务预算快照与执行计量（当前轮次）

- `backend/alembic/versions/0024_research_task_usage.py`、`backend/app/research/models.py`：为研究任务增加预算快照、搜索查询/结果数、输入/输出 Token、费用和当前步骤字段。
- `backend/app/research/service.py`：新增仅限有效租约当前 Worker 的增量记账函数；拒绝错误 Worker、过期租约和负数增量。
- `backend/app/research/schemas.py`、`frontend/src/api.ts`：同步任务状态返回字段，供后续 Worker/前端展示使用。
- `backend/tests/test_research_usage.py`：覆盖默认预算、有效记账、错误 Worker/过期租约拒绝和增量边界。
- 本切片不调用真实搜索 Provider、网页抓取或 LLM，不产生费用，不修改 `raw_signals`，不启动 Worker 或 scheduler。

### 16.26 第十二切片验收状态（2026-08-11）

- 已新增 `0024` 迁移；当前数据库为 `0024 (head)`。研究任务保存默认预算快照，并持久化搜索查询数、结果数、输入/输出 Token、费用和当前步骤。
- `record_task_usage` 只接受有效租约内的当前 Worker 增量记账；错误 Worker、过期租约和负数增量均被拒绝。
- 预算计量测试 4/4 通过；研究轨全量回归 52/52 通过；Ruff 通过；mypy 76 个源文件通过。
- app 镜像已重建并替换运行容器；app、PostgreSQL 均为 healthy，`/api/v1/system/health` 返回 `{"status":"ok","database":"ok"}`；scheduler 保持未运行。

第十二切片只完成任务级预算和用量的可审计存储与权限边界，不代表已经发生真实搜索或模型计费；实际 Provider/LLM 调用仍需后续单独接入和审批。

### 16.27 第十三切片实施范围：任务级预算预留闸门（当前轮次）

- `backend/app/research/service.py`：新增预算预留函数，在当前 Worker 有效租约内按预算快照检查预计用量；任一维度超限时抛出确定性预算异常，且不写入部分计数。
- `backend/tests/test_research_budget.py`：覆盖预留成功、查询/Token/费用超限、超限后计数不变和错误 Worker/过期租约拒绝。
- 本切片不新增预算来源、不调用搜索 Provider 或 LLM，不改变任务状态机，不启动 Worker 或 scheduler。

### 16.28 第十三切片验收状态（2026-08-11）

- 已新增 `reserve_task_usage` 确定性预算闸门：按任务预算快照检查查询、结果、输入/输出 Token 和费用的预计用量。
- 任一维度超限时抛出 `ResearchBudgetExceeded`，不写入部分计数；错误 Worker、过期租约和负数增量同样拒绝。
- 预算闸门测试 3/3 通过；研究轨全量回归 55/55 通过；Ruff 通过；mypy 76 个源文件通过。
- app 镜像已重建并替换运行容器；app、PostgreSQL 均为 healthy，`/api/v1/system/health` 返回 `{"status":"ok","database":"ok"}`；scheduler 保持未运行。

第十三切片只提供确定性预算预留，不会自动调用 Provider 或模型；后续 Worker 必须在产生外部调用前使用该闸门。

### 16.29 第十四切片实施范围：研究预算状态前端展示（当前轮次）

- `frontend/src/components/ResearchView.tsx`：在研究任务详情中展示查询/结果/Token 已用量与预算上限；有当前步骤时显示步骤标签。
- 展示仅使用任务详情 API 已返回字段，不新增请求、不执行预算计算、不提供修改预算入口。
- 本切片不接入搜索、网页抓取、LLM、Worker 或 scheduler。

### 16.30 第十四切片验收状态（2026-08-11）

- 研究任务详情已展示查询、结果、Token 已用量及预算上限；有执行步骤时显示当前步骤。
- 前端 TypeScript 检查通过；Vitest 1 个测试文件、6/6 测试通过；生产构建通过。
- Docker 构建阶段前端测试、类型检查和生产构建均通过；app 镜像已重建并替换运行容器。
- app、PostgreSQL 均为 healthy，`/api/v1/system/health` 返回 `{"status":"ok","database":"ok"}`；scheduler 保持未运行。

第十四切片只增加预算状态的只读展示，不新增轮询接口或外部调用，不改变预算闸门和风险评分链。

### 16.31 第十五切片实施范围：研究任务生命周期审计（当前轮次）

- `backend/app/research/router.py`：为研究任务创建、取消请求和报告草稿保存写入脱敏安全审计事件，记录角色用户、资源类型/ID和动作结果。
- `backend/tests/test_research_audit.py`：验证生命周期审计动作存在，且审计详情不包含研究主题正文、凭据或 Token。
- 本切片不新增审计 API、不改变权限、不接入外部 Provider/LLM，不启动 Worker 或 scheduler。

### 16.32 第十五切片验收状态（2026-08-11）

- 已覆盖三类研究生命周期审计事件：`research_task_created`、`research_task_cancel_requested`/`research_task_cancelled`、`research_report_draft_created`；审计详情不记录研究主题正文、Token 或凭据。
- 研究生命周期审计测试通过；研究轨回归 `56/56` 通过；Ruff 通过；mypy 通过（76 个源文件）。
- app 镜像已重建并使用 `docker compose up -d --no-deps --force-recreate app` 替换运行容器；app、PostgreSQL 均为 healthy。
- `/api/v1/system/health` 返回 `{"status":"ok","database":"ok"}`；scheduler 保持未运行。
- 本切片未发生外部 Provider、网页抓取或 LLM 调用，未产生外部费用，也未改变 `raw_signals`。

第十五切片完成后，研究任务创建、取消和报告草稿保存具备最小可审计闭环，并保持主题与用量等敏感内容脱敏。研究轨开发阶段0的本地安全基础已具备；ECS部署验证阶段0仍未完成真实 Provider、旁路抓取容器、ECS 容量测试、SBOM/漏洞扫描及 48 小时浸泡等上线前验收项。

### 16.33 第十六切片实施范围：研究业务审计最小查询（当前轮次）

- `backend/app/research/router.py`、`schemas.py`：新增仅限 `risk_admin`/`platform_admin` 的研究审计查询；只返回研究生命周期动作、资源标识、结果、时间和脱敏详情，并分页限制返回量。
- `backend/tests/test_research_audit.py`：验证分析员和 viewer 无权读取，管理员可读取研究事件，且响应不包含研究主题、请求 ID 或来源地址字段。
- 仅查询现有安全审计表，不新增数据库迁移、不新增外部调用、不启动 Worker、Provider、抓取容器或 scheduler。

### 16.34 第十六切片验收状态（2026-08-11）

- 研究审计查询仅允许具备 `business_audit_view` 的 `risk_admin`/`platform_admin` 访问；`risk_analyst` 和 `viewer` 均返回 403。
- 查询仅返回白名单研究生命周期动作，支持 `limit`/`offset` 分页，响应不包含研究主题、请求 ID 或来源地址字段。
- 研究轨回归 `57/57` 通过；Ruff 通过；mypy 通过（76 个源文件）。
- app 镜像已重建并使用 `docker compose up -d --no-deps --force-recreate app` 替换运行容器；app、PostgreSQL 均为 healthy；健康接口返回 `{"status":"ok","database":"ok"}`。
- scheduler 保持未运行；本切片未发生外部 Provider、网页抓取或 LLM 调用，未产生外部费用，也未改变 `raw_signals`。

### 16.35 第十七切片实施范围：Worker 执行阶段审计（当前轮次）

- `backend/app/research/worker.py`：为任务认领、成功、失败和取消完成写入脱敏审计事件；仅记录任务 ID、Worker ID 和结果状态，不记录任务主题、异常正文、Token 或凭据。
- `backend/app/research/router.py`：将新增执行阶段动作纳入研究审计白名单查询。
- `backend/tests/test_research_worker.py`：验证成功执行产生认领与完成审计事件，且详情不包含敏感正文。
- 本切片不启动常驻 Worker、不接入 Provider、网页抓取、LLM 或 scheduler。

### 16.36 第十七切片验收状态（2026-08-11）

- Worker 成功执行会写入 `research_task_claimed` 和 `research_task_succeeded`；失败、取消完成分别使用对应结果动作；详情只包含 Worker ID 与状态。
- 研究审计查询白名单已覆盖执行阶段动作；研究轨回归 `57/57` 通过；Ruff 通过；mypy 通过（76 个源文件）。
- app 镜像已重建并使用 `docker compose up -d --no-deps --force-recreate app` 替换运行容器；app、PostgreSQL 均为 healthy；健康接口返回 `{"status":"ok","database":"ok"}`。
- scheduler 保持未运行；本切片未发生外部 Provider、网页抓取或 LLM 调用，未产生外部费用，也未改变 `raw_signals`。

### 16.37 第十七切片全后端回归补充（2026-08-11）

- 全后端测试 `259/259` 通过；Ruff（`app` 与 `tests`）通过；mypy 通过（76 个源文件）。
- 修正两处测试环境隔离：天眼查默认测试不再继承持久化启用状态；bootstrap 弱密码测试在事务内清空已有用户。生产认证和天眼查实现未修改。
- app、PostgreSQL 均为 healthy；健康接口返回 `{"status":"ok","database":"ok"}`；scheduler 保持未运行。

### 16.38 第十八切片实施范围：Worker 异常状态审计回归（当前轮次）

- `backend/tests/test_research_worker.py`：补充 Worker 执行失败和执行期间取消两条路径的审计断言，验证分别产生 `research_task_failed` 与 `research_task_cancelled`。
- 验证异常正文和任务敏感内容不会进入 Worker 审计详情；不新增生产接口、数据库迁移或外部调用。

### 16.39 第十八切片验收状态（2026-08-11）

- Worker 成功、失败、取消三类完成路径均有认领与结果审计覆盖；失败异常正文不会进入任务错误或审计详情。
- 全后端测试 `259/259` 通过；Ruff（`app` 与 `tests`）通过；mypy 通过（76 个源文件）。
- 本切片不改变运行中 app 镜像，不启动 Worker、Provider、网页抓取、LLM 或 scheduler；现有 app、PostgreSQL 保持 healthy。

### 16.40 研究轨开发阶段0与 ECS部署验证阶段0门禁状态快照（2026-08-11）

已完成的本地安全基础：

- 研究任务持久化、幂等、权限隔离、取消、租约认领/续租、用量计量与确定性预算闸门。
- 受控单页读取、搜索 Provider 协议、引用回验、结构化报告草稿、人工确认边界。
- 前端研究入口、状态轮询、取消操作、预算状态展示。
- Worker 单次执行骨架及任务生命周期、审计查询和执行结果审计。
- 全后端测试 `259/259`、Ruff、mypy 通过；app/PostgreSQL healthy。

仍待完成的 ECS部署验证阶段0外部门禁：

- RSSHub、Crawl4AI 旁路容器及真实网页读取。
- Bocha/Tavily 等搜索 Provider 的 ECS 可达性、延迟、质量和计费实测。
- 真实手动研究任务端到端、LLM 调用与费用验证。
- ECS 整机容量、48 小时浸泡、SBOM 和漏洞扫描。

在上述门禁完成前，研究轨开发阶段0状态为“本地安全基础完成”，ECS部署验证阶段0状态为“外部 Spike 部分完成”，不得宣称已具备生产研究能力。

### 16.41 第十九切片实施范围：真实外部读取与搜索 Provider 配置（当前轮次）

- `backend/app/config.py`：增加 `SEARCH_PROVIDER`、`SEARCH_API_KEY`、`SEARCH_BASE_URL` 和 `SEARCH_TIMEOUT_SECONDS` 配置读取；默认 Provider 为 `none`。
- `backend/app/research/search.py`：增加 Bocha/Tavily 的显式 Provider 适配器；未配置 Provider 或 Key 时返回未配置，不发起网络请求；响应统一转换为候选 URL 并继续执行 HTTPS、凭据和公网地址校验。
- `.env.example`、`deploy/.env.production.example`：增加脱敏配置模板，不写入任何真实 Key。
- `backend/tests/test_research_search.py`：使用 MockTransport 验证未配置探针、Tavily/Bocha 响应映射和请求边界；不消耗真实搜索额度。
- 本轮外部读取仅使用公开官方页面、公开 RSS 和临时 RSSHub digest 容器；不接入平台数据库，不启动常驻抓取服务。

### 16.42 第十九切片外部 Spike 验收状态（2026-08-11）

- 通过现有受控单页读取器读取 `https://www.gov.cn/`：HTTP 200，最终 URL 未漂移，正文摘要长度受限。
- 读取公开 BBC RSS：HTTP 200，`text/xml`，RSS 内容识别成功。
- `https://127.0.0.1/` 被 SSRF 校验拒绝；`httpbin` 重定向测试因目标网络超时失败，未继续重试。
- RSSHub digest `diygod/rsshub@sha256:c7622431189ad84847287dc07478df9ca7494f33987d322f3e3e2b11cd75a2cd` 临时容器健康；公开 GitHub 路由返回 HTTP 200、XML/RSS；临时容器已删除，未写入 compose。
- Crawl4AI 固定候选 `0.6.3` 标签不存在，`0.7.4` 拉取超时；未使用 `latest`，Crawl4AI Spike 待网络/镜像门禁解决后再试。
- 当前环境无 `SEARCH_PROVIDER`/搜索 Key，Provider 未配置探针返回 `provider_configured=false`，未发生 Bocha/Tavily 真实请求或费用。
- Provider 测试 `15/15` 通过；全后端测试 `263/263` 通过；Ruff、mypy 通过（76 个源文件）；app、PostgreSQL healthy。

### 16.43 第十九切片 Provider 网络探针补充（2026-08-12）

- 在 app 容器内不携带 Key 访问 `https://api.tavily.com/`：HTTP 200，JSON 响应。
- 在 app 容器内不携带 Key 访问 `https://api.bochaai.com/`：HTTP 404，JSON 响应；DNS/TLS/网络链路可达，根路径不存在不代表搜索接口失败。
- 两次探针均未发送搜索查询、企业名称或 API Key，不产生 Provider 计费；真实搜索仍待录入已批准的测试 Key 和单次预算后执行。

### 16.44 第二十切片实施范围：最小调度闭环（当前轮次）

- 监控轨不新增调度系统：继续使用数据源独立 cron 和每分钟注册表刷新；迁移仅把仍为旧默认值的中央气象台周期从 30 分钟调整为 5 分钟，不覆盖管理员已经修改的周期。
- 研究轨复用现有 Scheduler 和 `research_tasks`：默认日报每天 08:00、周报每周一 08:30（Asia/Shanghai），按日期/自然周幂等创建 `queued` 任务。
- 日报/周报只有在主题和归属管理员均配置时才注册；归属账号必须是启用的 `risk_admin` 或 `platform_admin`。
- Web API 只允许创建 `manual` 即时任务，拒绝客户端伪造 `daily`/`weekly`；管理员仍可手动即时触发。
- 本切片不启动 research Worker，不执行搜索、网页读取或模型调用，不调用任何外部付费 API。

### 16.45 第二十切片验收状态（2026-08-12）

- Compose 配置解析通过；Scheduler 未运行，现有 app 与 PostgreSQL 保持 healthy，未替换运行容器。
- 调度/研究定向回归 `65/65` 通过；全后端回归 `266/266` 通过；Ruff 通过；mypy 通过（76 个源文件）。
- Alembic 迁移链保持单一 `0025 (head)`；未对当前数据库执行迁移。
- 测试证明同一日报周期只创建一个 `queued` 任务，缺少主题或归属管理员时不创建，手动 API 仅接受 `manual` 类型。
- 验收期间未启动 Scheduler、research Worker 或搜索 Provider，未发生外部采集、网页读取、LLM 调用或付费 API 请求。

### 16.46 第十九切片 Crawl4AI 镜像门禁与单页 Spike 补充（2026-08-12）

- 已解决 Crawl4AI 镜像获取问题：通过 Docker Hub API 查询有效标签后，固定使用官方 digest `unclecode/crawl4ai@sha256:bd36741e7bdd35ddc1a05d9183e1d6d8cefb61dd640d944a25d026b76e917690`，对应 Crawl4AI `0.9.2`（amd64 镜像约 1.62GB）；未使用 `latest`。
- 临时容器 `srm-spike-crawl4ai` 仅绑定 `127.0.0.1:11235`，注入随机临时认证参数，健康检查通过；日志确认认证门禁、浏览器池和 Crawl4AI `0.9.2` 正常启动。
- 认证调用 `POST /crawl` 读取 `https://www.gov.cn/`：HTTP 200、最终 URL 为 `https://www.gov.cn/`、页面状态码 200、HTML 约 65.3KB、原始 Markdown 约 14.6KB；服务端处理约 0.65 秒，单次请求耗时约 0.73 秒。
- 单页读取后的容器观测值：CPU 约 13%，内存约 489.8MiB/7.67GiB（6.23%）；服务端报告峰值内存约 151.3MB。该数据仅作为当前云服务器条件下的 Spike 参考，正式上线仍需按目标并发做容量和浸泡测试。
- Spike 完成后已删除临时容器；本次未修改 Compose、未启动常驻 Crawl4AI、未写入平台数据库，也未产生 Provider/LLM 费用。后续若正式接入，必须沿用 digest 固定版本、内网绑定、认证、并发/超时/资源上限和 SSRF 校验。

### 16.47 博查测试 Key 首次真实请求（2026-08-12）

- 根目录 `.env` 已配置 `SEARCH_PROVIDER=bocha`、非空 `SEARCH_API_KEY`，未配置自定义 `SEARCH_BASE_URL`；仅重建 app 容器加载环境变量，未启动 Scheduler 或重建 PostgreSQL。
- 先使用公开查询“ 中国政府网 供应链 风险 政策 ”执行 1 次真实博查搜索请求；网络链路已到达 `https://api.bochaai.com/v1/web-search`，但返回 HTTP 401（未授权），尚未获得可用搜索结果。
- 再严格按博查官方示例参数（`oneYear`、`summary=true`、`count=8`）执行 1 次原始 HTTP 对照请求，仍返回 HTTP 401；由此排除 `httpx` 封装、请求体字段和 `freshness` 参数差异导致的问题。
- 当前证据表明容器环境注入和代码鉴权格式已生效；401 更可能是测试 Key 未激活、Key 内容/权限不匹配或账号侧授权状态问题。共执行 2 次对照请求后停止，避免无效消耗额度；待在博查控制台确认 Key 状态后再执行单次复测。

### 16.48 博查有效 API Key 鉴权与结果清洗复测（2026-08-12）

- 用户已将资源包 ID 更换为真实博查 Web Search API Key；根目录 `.env` 中 Key 非空、无引号和空白污染。仅重建 app 容器加载新环境变量，app 与 PostgreSQL 保持 healthy，Scheduler 未启动。
- 新 Key 阶段首次平台请求已通过博查鉴权并收到响应，但平台结果清洗在 `_sanitize_result_url` 处拒绝了至少一个非 HTTPS URL，未向上层返回结果。
- 随后按官方示例参数执行原始请求，博查返回 HTTP 200、2 条结果，观测到的 URL 均为 HTTPS；再次执行平台封装请求时仍因实时结果中出现非 HTTPS URL而被拒绝，说明 Endpoint、Bearer 鉴权和响应到达均正常，当前剩余问题是 Provider 结果协议与平台 HTTPS 安全边界的兼容性。
- 新 Key 阶段共执行 3 次请求；未启动研究 Worker/Scheduler，未改变 `raw_signals`。在明确安全处理策略前，不放宽 HTTPS/公网 URL 校验，也不继续重复请求。

### 16.49 博查结果清洗兼容修复与最终复测（2026-08-12）

- `backend/app/research/search.py` 调整 `_sanitize_response`：单条候选 URL 不符合 HTTPS、公网、无凭据约束时直接丢弃，继续保留同批合法结果；若响应非空但没有任何合法结果，仍抛出错误，保持安全失败边界。
- `backend/tests/test_research_search.py` 新增混合 URL 回归覆盖，并扩展 Bocha 嵌套结果测试；搜索模块测试 `16/16` 通过。
- 仅重建 `app` 镜像和 `app` 容器，未启动 Scheduler、未重建 PostgreSQL；全后端回归、Ruff、mypy 均通过，app 与 PostgreSQL healthy。
- 使用真实博查 Key 查询“阿里巴巴2024年ESG报告的重点”：Provider 鉴权成功，返回 1 条合法 HTTPS 结果，结果标题为“阿里巴巴发布2024年ESG报告：保持前瞻、保持善意、保持务实”，耗时约 0.55 秒；未输出正文或 Key。
- 本次修复没有放宽 SSRF/公网 URL 校验，也未写入 `raw_signals`；博查 API 目前仅完成单次手动调用验证，Scheduler/研究 Worker 仍保持停用。

### 16.50 手动研究端到端 Spike 与 AI 结构化失败（2026-08-12）

- 按用户批准执行 1 次最小端到端链路：博查搜索“阿里巴巴2024年ESG报告的重点”→ 内部网络临时 Crawl4AI 单页读取→ Qwen `qwen3.6-plus` 结构化分析；临时 Crawl4AI 使用固定 digest、未映射宿主端口，测试结束后已删除。
- 搜索和网页读取阶段已到达：博查返回候选，Crawl4AI 单页读取成功并提供正文摘要；模型请求已发出，但 Qwen 返回 `event_type=corporate` 与不兼容 `event_subtype` 的组合，Pydantic 语义校验失败。原实现将该确定性校验错误视为可重试错误，已在内置重试后停止，未生成报告草稿。
- 本次失败未写入 `research_reports` 或 `raw_signals`；未启动 Scheduler/常驻 Worker。此次批准的真实模型 Spike 已消耗模型调用及其重试额度，后续不再未经批准重复调用。
- 本地修复：`backend/app/ai/providers.py` 补充完整的事件大类/细类映射提示，并将 `ValidationError` 改为不可重试的 `AIProviderError`，避免结构化语义错误重复计费；`backend/tests/test_ai.py` 新增“不重试语义校验错误”回归。
- 修复后 AI/搜索定向测试 `22/22` 通过，全后端回归通过，Ruff 通过，mypy 通过（76 个源文件）；app、PostgreSQL healthy。下一次真实模型复测仍需单独批准。

### 16.51 手动研究端到端复测成功（2026-08-12）

- 针对上次模型细类组合不兼容问题，`backend/app/ai/providers.py` 增加保守兼容：当 `event_type` 合法但 `event_subtype` 与其映射不兼容时，将细类置为 `null` 后继续校验；非法大类、严重度、证据等其他结构错误仍失败且不重试。新增回归测试，AI/搜索定向测试 `23/23` 通过。
- 按用户继续批准执行 1 次最终端到端 Spike：博查搜索→内部网络临时 Crawl4AI 单页→Qwen `qwen3.6-plus`→结构化报告草稿与引用校验；未写入数据库、未启动 Scheduler/常驻 Worker。
- 博查返回 1 条结果；Crawl4AI 读取 `https://fund.eastmoney.com/a/202407223136849076.html` 返回 HTTP 200，最终 URL 未漂移，正文摘要截取 6000 字符，端到端网页阶段耗时约 83.82 秒（含服务准备/读取）。
- Qwen 调用成功：`event_type=other`、`event_subtype=null`、建议严重度 `low`、置信度 `0.95`、证据句 3 条；报告结构校验通过，引用 `verified=true`。模型输出仅作为参考摘要，不写入 `raw_signals`。
- 本次临时 Crawl4AI 容器仅加入 Docker 内部网络、未映射宿主端口，完成后已删除；app、PostgreSQL healthy。研究轨开发阶段0的“单次手动研究端到端”门禁完成，但 ECS部署验证阶段0的 48 小时浸泡、ECS 容量、SBOM/漏洞扫描和正式 Worker/调度接入仍未完成。

### 16.52 ECS部署验证阶段0上游镜像核查与候选路径取舍（2026-08-14）

- RSSHub `Docker Release` 工作流仍为 active，最新可见成功运行是 #8221（2026-08-12，提交 `e086c17fa01bfbedf0dd4f4ee0b79c35a96fba61`）；工作流发布 Docker Hub/GHCR 多架构镜像，但未形成可公开核验且通过本项目安全门禁的新 digest。仓库当前没有正式 Git Tag，GHCR 版本接口要求认证，Docker Hub Registry 本轮不可达。
- RSSHub `master`（`7fcec5001ebb9b25fe9de1435f5aff216746ab84`，2026-08-13）晚于该 Docker Release；未发现对应的新镜像成功证据。现有固定 digest 的 Critical `17`、High `53` 仍有效。
- Crawl4AI 最新正式 Release/Tag 仍为 `v0.9.2`（2026-07-15）；固定 digest 的复扫为 Critical `44`、High `604`。其中 32 个 Critical 没有 Trivy 修复版本，不能通过简单升级依赖满足零高危门禁。
- 结论：不启动 RSSHub/Crawl4AI，不创建 `deploy/.env.stage0`，不修改 `compose.stage0.yaml`。派生加固镜像只能建立临时候选并重新完成 SBOM、漏洞、功能和出网边界复验；不能直接替换生产或 ECS部署验证阶段0固定 digest。
- 若需继续推进无旁路容器的 ECS部署验证阶段0 Spike，优先使用现有 `backend/app/research/web.py` 受控 HTTPS 单页读取器配合已验证的搜索 Provider，范围限定为公开静态 HTML/RSS；不支持 JavaScript 渲染页面，不能等同于 Crawl4AI，阶段状态只能记录为“受控直连 Spike”，不得宣称研究旁路能力上线。

核查入口：

- RSSHub Docker Release 工作流：<https://github.com/DIYgod/RSSHub/actions/workflows/docker-release.yml>
- RSSHub 最新工作流运行：<https://github.com/DIYgod/RSSHub/actions/runs/31563109839>
- Crawl4AI v0.9.2 Release：<https://github.com/unclecode/crawl4ai/releases/tag/v0.9.2>

### 16.53 ECS部署验证阶段0固定镜像临时风险接受（2026-08-14）

- 负责人已接受 RSSHub 固定 digest（Critical `17` / High `53`）与 Crawl4AI 固定 digest（Critical `44` / High `604`）的已知漏洞风险，仅用于 ECS部署验证阶段0的内部低并发旁路 Spike；不构成生产长期豁免。
- 启动边界：仅限现有 ECS、内部网络、单实例、RSSHub `512 MiB / 0.5 CPU`、Crawl4AI `1536 MiB / 1 CPU`、浏览器与队列并发均为 1；不新增宿主端口、特权权限、主机目录挂载、研究 Worker、自动调度或批量采集。
- 本轮观察仅验证容器健康、配置加载、网络暴露和整机资源；任何搜索 Provider、网页抓取、模型调用或风险信号写入均需单独批准，避免将漏洞风险接受扩大为计费或业务自动化授权。
- 观察期从实际启动起 48 小时。OOM、异常退出、MVP 健康异常、端口越界、资源余量低于既定阈值或未经批准的外部调用均触发立即停止旁路容器，并在结束或上游修复镜像可用时重新评审。

### 16.54 ECS部署验证阶段0旁路已启动，48小时观察开始（2026-08-14）

- ECS 已使用固定 RSSHub/Crawl4AI digest 启动旁路容器；Crawl4AI 有效启动时间为 `2026-08-14T20:24:00+08:00`，计划观察截止 `2026-08-16T20:24:00+08:00`。任何停止、重建或健康失败均使观察期重新计时。
- Token 仅在 ECS `deploy/.env.stage0` 中生成，权限为 `600`，未输出或提交。首次 Token 生成受 PowerShell 转义影响写入非预期短值，已在验证前停止并重建 Crawl4AI；有效 Token 为重新生成的随机值。
- 启动验收：Crawl4AI Docker health 为 healthy，RSSHub 内部 HTTP `200`；两服务只加入 `supplier-risk-monitoring_internal`，无宿主端口映射，重启次数为 0。MVP 健康接口仍为 HTTP `200`。
- 初始资源：RSSHub 约 `195.7 MiB / 512 MiB`、Crawl4AI 约 `313.8 MiB / 1.5 GiB`、整机可用内存约 `1.8 GiB`、无 Swap。尚未执行网页抓取、搜索 Provider、LLM 或风险信号写入；这些业务验证仍须单独批准。

### 16.55 ECS部署验证阶段0本机最小真实读取验证与边界复核（2026-08-14）

- 在本机 Docker 内部网络中，RSSHub 读取公开 GitHub 问题流成功：HTTP `200`、XML 响应 `170180` 字节、耗时约 `5.20` 秒；Crawl4AI 以内部认证读取公开 `https://www.gov.cn/` 成功：HTTP `200`、Markdown `14841` 字符、耗时约 `6.61` 秒。
- 两次请求均为人工单次读取，不使用平台数据库、研究 Worker、Scheduler、搜索 Provider 或 LLM；不保留网页正文，认证 Token 未输出。读取后 RSSHub 与 Crawl4AI 重启次数均为 `0`，Crawl4AI 保持 `healthy`，内存分别约为 `209.7 MiB` 与 `527.5 MiB`。
- 该结果只证明固定旁路镜像在本机的内部认证、公开单页读取和资源上限路径可工作；RSSHub XML 检测不能替代来源授权审查，Crawl4AI 成功读取也不能替代 robots、重定向、SSRF、403/429、超大响应、最终主机租约和 ECS 48小时共存验证。
- 本机 `research-worker` 是负责人为实机测试主动启动的，处于 `topic_source_discovery` 模式。任务 `684` 在 Worker 停止前已经完成：状态 `succeeded`，执行了 `1` 次搜索、获得 `6` 个候选、成功保存 `2` 个来源；任务账本成本和输入/输出 Token 均为 `0`。日志中的 4 条候选来源读取失败对应未能读取的候选，不代表研究任务失败。
- Worker 停止发生在任务 `684` 已完成之后，因此未中断这次实机测试；其当前状态为 `Exited (0)`。后续新的真实研究任务仍应由负责人从前端显式点击“开始研究”，使每次 Provider 调用和网页读取均有明确的任务授权与审计记录。

ECS部署验证阶段0当前状态：本机“最小真实读取”子项完成；ECS 旁路 48 小时观察继续按 `2026-08-16T20:24:00+08:00` 截止时间执行。本机结果不得将 ECS部署验证阶段0标记为完成，剩余的 ECS 资源/稳定性证据、外部边界异常行为与正式执行器接入仍需按既定审批推进。

### 16.28 本机自动发现与报告草稿实机验收（2026-08-15）

- 负责人通过前端显式创建并开始任务 `685`，本机 Worker 以 `topic_source_discovery` 完成一次真实闭环：搜索 `1` 次、候选计数 `6`、成功受控读取并保存来源 `2` 个；未自动启动第二搜索 Provider。
- 既有 AI Provider 随后生成报告草稿 `86`，模型为 `qwen3.6-plus`。任务记录实际 usage：输入 Token `1407`、输出 Token `2709`、成本字段 `0`（当前未配置价格换算，`0` 不表示外部调用免费）。
- 报告状态为 `draft/pending`，含 `2` 条事实、`1` 条推断、`0` 条预测和 `2` 条平台回验引用；两条来源 HTTP 均为成功。审计顺序完整：任务创建、显式开始、Worker 认领、报告草稿生成、任务完成。
- 本轮没有写入 `raw_signals`、风险评分或自动转正。该实机结果验证本机“主题到引用报告”的最小闭环，不替代报告内容的人工作业审核、来源质量评估、费用核算、ECS 稳定性或正式生产发布验收。

### 16.56 监控信号融合与执行可视化开发范围（2026-08-15）

- 负责人已批准先落实文档，再开发后端“监控轨上下文融合 + 可回放执行事件流”；效果图已确认，前端已按确认方案实施。
- 专项方案、取舍、接口和完成定义见[《研究轨监控信号融合与执行可视化开发计划》](docs/研究轨监控信号融合与执行可视化开发计划.md)。
- 监控轨上下文仅按任务显式 `supplier_scope` 读取当前提醒及其已成功分析、无需复核、带公开 HTTPS 原始来源的信号；无供应商范围时不读取全库信号。
- 监控信号复用 `research_sources` 与 `research_citations` 形成任务快照；研究轨仍不直接改变 P1-P4，也不绕过人工转正闸门。
- 动态可视化数据由新增的只追加任务事件流提供，第一版使用增量轮询，不引入 WebSocket、SSE、事件总线或图数据库。
- 本轮后端开发和测试不得启动自动调度，不执行真实搜索、网页读取或模型调用；外部调用边界仍按既有逐次授权执行。

### 16.57 监控信号融合与任务事件流后端验收（2026-08-15）

- 已新增 Alembic `0031` 和 `research_task_events` 只追加事件流，迁移链为单一 `0031 (head)`；本地数据库已完成 `0030 -> 0031` 加法迁移，未重建或重启现有 app/PostgreSQL。
- 研究任务按显式 `supplier_scope` 纳入最多 5 条当前、成功分析且无需复核的监控信号，并复用研究来源和引用模型形成可追溯快照；无供应商范围时不读取全库信号。
- 自动发现结果会与任务内监控证据按 URL、正文哈希去重；报告仍只使用平台已回验引用，未增加模型调用轮次或绕过人工转正闸门。
- 新增 `GET /api/v1/research/tasks/{task_id}/events` 增量接口，按事件 ID 游标返回脱敏执行节点，并沿用任务所有权隔离。
- 研究轨回归 `74/74`、全后端回归 `284/284`、Ruff、mypy（77 个后端源文件）和 `git diff --check` 均通过；验证使用 Fake Provider，未发生真实搜索、网页读取、模型调用或付费 API 请求。
- `alembic check` 仍受既有迁移环境未加载风险模型及 PostGIS/Tiger 扩展表误判影响，本轮未扩大范围修复；实际升级和单一 head 已验证。
- 动态网络图效果图已获负责人确认，前端已进入并完成实现，验收记录见 16.58。

### 16.58 研究执行图前端实施与本地 app 验收（2026-08-15）

- 创建任务区已增加供应商搜索与多选，任务载荷写入 `supplier_scope`；未选择时明确提示不会读取全库监控信号。
- 前端通过事件 ID 游标每 2.5 秒增量刷新运行中任务事件，并同步刷新任务状态和研究来源；监控轨来源使用橙色，公网来源使用蓝色。
- 新增研究执行图：桌面端展示任务、监控上下文/信号、公网检索/候选、证据池和报告节点及动态连线，窄屏改用相同真实事件的时间线；节点详情支持状态、计数、摘要和原始来源跳转。
- 实现仅复用现有 React、Tailwind、Motion 与 Lucide，未新增 npm 依赖；支持 reduced-motion，且不模拟不可观测的模型思维链。
- 前端单测 `6/6`、TypeScript lint、Vite 生产构建通过；Docker 构建阶段再次通过上述门禁。Vite 仅有既有主包超过 500 kB 的非阻断提醒。
- 仅重建本地 `app`，PostgreSQL、RSSHub、Crawl4AI 未重建，Scheduler 保持停止；健康接口返回 `status=ok`、`database=ok`。
- 浏览器自动化会话无有效登录状态，本轮未读取 `.env` 或绕过认证；已确认新登录页资源可加载，登录后的桌面网络图与窄屏时间线仍待有效用户会话下补充实景视觉验收。

### 16.59 受控研究 Agent 与全供应商定期研究方案登记（2026-08-16）

- 经能力边界复核，当前 `topic_source_discovery` 仍是“单次搜索 → 固定读取 → 单次报告”的受控流水线，不得继续表述为已经落地通用或受控研究 Agent。
- 已确定以 LangGraph 作为唯一研究流程编排器，继续复用现有 AI Provider 和原生 Pydantic 结构化校验；不引入其他模型或 Agent 运行时，避免形成第二套工具循环、Provider封装、重试和用量统计。
- 手动任务、月报和预留周报必须复用同一个研究图、工具白名单、安全治理、预算和引用回验；差异只限触发方式、周期窗口、供应商范围和预算模板，禁止另建周报执行器。
- 按约 150 家供应商和博查每月 1000 次额度测算，周报每家每周最多 2 次搜索约需 1305 次/月；即使每家每周 1 次也约需 653 次/月，会明显挤压手动研究和异常余量。因此当前规划以月报为默认周期能力，周报复用同一批次流程但管理员开关默认关闭，并继续受研究轨部署总开关约束。
- 既有 Scheduler 创建 `supplier_scope=[]` 的单个日报/周报任务不能满足供应商范围研究要求。月报调整为“一个月度批次 + 每个选定供应商一个独立子任务 + 一个组合报告”，默认按 `RESEARCH_MONTHLY_SUPPLIER_SCOPE=limited` 和 `RESEARCH_MONTHLY_SUPPLIER_LIMIT=100` 从启用供应商中稳定选择最多 100 家；保留 `scope=all` 的显式全量 150 家开关，不增加供应商核心字段，不随机或静默超范围执行。
- 月报子任务使用单供应商范围，单独预算、重试和失败隔离；初期由单 `research-worker` 串行削峰。所有选定供应商子任务到达终态后生成组合月报，组合结论只能继承子任务已回验引用，不重新抓取或伪造证据。
- 月报按每家基础 2 次搜索、证据不足时最多第 3 次计算，默认 100 家为 200～300 次/月；全量 150 家为 300～450 次/月。月报批次硬上限 450 次，至少保留 300 次给手动研究、250 次给异常与安全余量，并在调用前通过数据库全局额度账本原子预占。
- 若领导批准增资，管理员可在博查月额度和平台额度账本更新后手动开启周报。以150家、5个周触发点、每家最多2次搜索，并保留月报450次、手动300次和安全余量250次计算，月额度至少需要2500次；系统按实际供应商数量和当月触发次数动态预检，不满足时拒绝开启。
- 监控轨继续承担高频风险发现；既有日报仅作历史兼容。周报开启后也必须按每供应商子任务执行，不允许恢复空范围全局研究。
- 专项架构、150 家供应商容量测算、周报双重开关、数据模型、调度语义、文件级任务、测试、完成定义和回滚方案见[《受控研究 Agent、全供应商月报与可选周报实施方案》](docs/受控研究Agent与全供应商定期研究实施方案.md)。
- 本轮规划更新将默认范围从“全部启用供应商”纠正为“最多 100 家稳定选定供应商”，并保留全量开关；后端实现、测试和配置示例随后分阶段落实，不修改 ECS，不执行真实搜索、网页读取、模型调用，不产生外部费用。

### 16.60 LangGraph 最小迁移图后端基线（2026-08-16）

- 已在 `backend/pyproject.toml` 固定引入 `langgraph==1.2.11`，并新增 `app/research/graph.py`。最小图先编排“监控轨证据快照 → 既有受控主题发现”；当前补充搜索分支升级为 `research-graph-v2`。
- `RESEARCH_ORCHESTRATOR` 已限定为 `legacy|langgraph`，默认 `legacy`；Compose 仅把该值传递给 research-worker。LangGraph 路径会写入 `agent_graph_started` / `agent_graph_completed` 事件，但不会把固定流水线伪装为已具备自主研究能力。
- 隔离图测试使用非持久化 `MemorySaver`；`langgraph` Worker 路径已接入 PostgreSQL checkpoint。受控补充搜索已在 16.63 实施；月报批次和周报开关仍按后续切片实施。
- 验证已在隔离临时 PostgreSQL 中通过研究图、Runner、事件流和 Worker 定向回归 `14 passed`；当前源码的 Ruff 及全后端 mypy（`78` 个源文件）均通过。全部测试使用 Fake Provider/Mock，不访问搜索、网页或模型服务；未改动 ECS。

### 16.61 工具调用幂等账本后端基线（2026-08-16）

- 已新增 Alembic `0032` 与 `research_tool_runs`。同一任务的 `web_search`、`public_page_read`、`report_generation` 通过 `UNIQUE(task_id, action_id)` 互斥，动作参数只以 SHA-256 哈希存储；账本不保留 URL、网页正文、模型提示词或任何凭据。
- Runner 已在真实调用前认领动作，并在来源或报告写入后登记成功结果。恢复时若动作已成功且对应来源/报告存在则复用；若上次调用尚未完成、已失败或成功记录缺少可复用结果，则显式停止以避免重复搜索、读取或模型费用。
- 新增账本回归覆盖：相同参数顺序产生同一动作 ID；重复执行同一任务不会再次调用 Fake 搜索、页面读取或报告生成。隔离数据库定向回归 `16 passed`、全后端 pytest、Ruff、mypy 均通过；本地 `supplierriskmonitoring-app:latest` 镜像已重建，未重启 App、Worker、Scheduler、PostgreSQL 或 ECS 服务。

### 16.62 PostgreSQL checkpoint 后端基线（2026-08-16）

- 已固定增加官方 `langgraph-checkpoint-postgres==3.1.2`，仅在 `RESEARCH_ORCHESTRATOR=langgraph` 时创建 PostgreSQL checkpointer；legacy 执行器继续完全独立。
- checkpoint 线程标识固定为 `research-task-{task_id}-{graph_version}`，且只保存图状态与节点进度；图结构变更会使用新线程，避免旧 checkpoint 被错误续跑。任务租约、预算、费用、来源、引用、报告、任务事件和工具调用幂等仍分别由既有业务表负责，禁止将 checkpoint 当作审计或费用事实来源。
- SQLAlchemy 的 `postgresql+psycopg://` 地址仅在交给官方 checkpointer 时转换为 psycopg/libpq 所需的 `postgresql://`，不改变应用既有连接配置。迁移 `0036` 在进程启动前预创建官方表、索引并登记版本；运行时 `setup()` 只做幂等确认，避免业务事务与 `CREATE INDEX CONCURRENTLY` 相互等待。
- 已在隔离 PostgreSQL 验证：任务线程可持久读取 checkpoint；在 `monitoring_context` 节点后中断，再使用同一 `thread_id` 恢复时仅继续 `controlled_discovery`；账本中的 `running/failed` 动作不会因 checkpoint 而重放。外部响应未持久化的未知状态仍安全停止。

### 16.63 证据不足的一次受控补充搜索切片（当前轮次）

- 仅为 LangGraph 图加入平台确定性条件边：首轮证据少于 1 条、任务仍有 `max_queries` 余额且未补充过时，派生固定查询“`{主题} 风险 公告`”并最多再搜索一次；不让模型生成查询、选择工具或继续循环。
- 需将既有主题发现拆为可复用的单轮发现与报告生成。首轮和补充轮均继续使用任务预算、工具账本、HTTPS 读取器、来源/引用去重、事件流与 PostgreSQL checkpoint；每轮搜索使用不同查询，因此账本动作 ID 不冲突。
- 补充后仍没有已回验证据、或额度不允许补充时，写入不调用模型的“公开证据不足”草稿并停止；不伪造风险结论、不自动降级到第二 Provider、不写入 `raw_signals`。
- 图结构升级为 `research-graph-v2`，checkpoint 线程加入图版本；旧 `v1` checkpoint 不会被新图续跑。
- 验收覆盖：证据充足只搜索一次；首轮无有效证据只补充一次；补充后仍不足时正常生成不足草稿；预算耗尽不补充；checkpoint 恢复不重复已完成动作；legacy 路径保持原行为。定向隔离回归 `15 passed`，Ruff 和本切片 mypy 均通过，全部使用 Fake/Mock，不发生真实搜索、抓取或模型调用。
- 完整 mypy 已通过（78 个源文件）。完整 pytest 仍被既有迁移缺失 `suppliers.registration_address` 列阻断；完整 Ruff 仍被无关的 `tests/test_scheduler_registry.py` 导入排序阻断，本切片未扩大范围修复。

### 16.64 月报批次扇出切片（当前轮次）

- `research_batches` 记录自然月批次、供应商快照、预算快照和状态计数；`research_tasks` 增加 `monthly` 类型与可空 `batch_id`，保持手动/daily/weekly 历史兼容。
- Scheduler 新增月报注册开关，默认关闭；开启后按默认最多 100 家的范围配置，或显式 `scope=all` 全量配置，创建一个幂等批次和每供应商一个已授权子任务，不创建空范围全局研究任务。
- Worker 允许已由 Scheduler 明确授权的 monthly 子任务进入既有受控研究图；手动任务的显式开始语义保持不变。
- 验收使用 Fake 供应商和隔离 PostgreSQL，覆盖 150 家启用供应商下默认 100 家快照、全量开关 150 家快照、重复触发、停用供应商边界、单任务失败隔离和批次计数；本轮不调用 Provider、网页或模型，不实现组合报告与全局额度预占。

### 16.65 月报组合报告与批次只读 API（当前轮次）

- `research_reports` 支持 `task_id` 或 `batch_id` 二选一；批次报告仅在全部子任务进入终态后由确定性聚合器创建，并通过部分唯一索引保证同一批次最多一份。
- 聚合器只继承子报告中的已回验引用和结论，给结论/引用加上批次与任务前缀避免 ID 冲突；不重新搜索、抓取或调用模型。
- 新增批次列表/详情只读接口，展示周期、供应商快照、子任务状态和组合报告；接口沿用现有研究任务权限隔离。
- 定向测试覆盖：全部成功、部分失败、无可继承证据、重复完成幂等、批次权限隔离；不启用月报调度，不发生外部调用。

### 16.66 Provider 月度额度原子预占（当前轮次）

- 新增 `research_provider_quota_periods` 与 `0038` 迁移，按 Provider 和自然月记录限额、定时/手动预占、已用次数。
- Runner 在 `web_search` 外部调用前先执行数据库行锁预占，并把预占元数据写入工具账本；工具成功或未知失败统一结算为已用，避免重复计费和并发超额。
- Fake Provider 和未配置 Provider 不触发全局额度；默认 `SEARCH_MONTHLY_LIMIT=1000`，月报开关继续关闭。
- 定向测试覆盖并发预占、上限拒绝、成功结算、失败保守计费、重复结算幂等和手动/定时分类；不执行真实搜索。

### 16.67 遗留工具调用与额度预占恢复对账（当前轮次）

- 新增清理对账：仅处理已超过 `RESEARCH_TOOL_RUN_STALE_SECONDS` 且任务租约已过期/已结束的 `running` 工具账本，租约仍有效的长任务不受影响。
- Provider 预占在无法确认请求是否离开进程时按保守策略结算为 `used`；工具账本标记 `failed/stale_reconciled` 并追加脱敏事件，避免额度悬挂和任务永久等待。
- 对账使用行锁、状态检查和 `settled` 标记保证多次 Scheduler 执行幂等；无 Provider 预占的页面读取/报告动作同样结束为可审计失败。
- 只使用 Fake/隔离数据库回归，不执行真实搜索、网页读取、模型调用或 ECS 操作。

### 16.68 周报运行时开关与关闭态额度/容量预检（当前轮次）

- 新增 `research_schedule_configs` 和管理员读写接口，周报配置默认关闭；保存批准额度、批准人、批准时间、说明及预算模板，写操作沿用 CSRF 与 `research_schedule_manage` 权限。
- 后端按真实启用供应商数、当月 cron 触发点和每供应商 `max_queries` 计算最坏搜索量，并叠加月报 450、手动 300、安全余量 250 的保留额度；Provider 未配置、余额不足或批准额度不足时拒绝开启。
- Scheduler 每分钟动态刷新周报 Job，开关关闭/预检失败时不注册；启用后复用同一周期批次扇出，不创建空范围全局任务。
- 回归只使用 Fake/隔离数据库，覆盖默认关闭、权限、额度拒绝、批准额度不足、启用配置持久化和运行时批次入口；不执行真实周报或外部调用。

### 16.69 月报额度预检与预算快照对齐（当前轮次）

- 月报 Scheduler 创建批次前复用同一 Provider 月度额度账本预检；Provider 未配置、月度余额不足或启用供应商数超过月报预算上限时，不创建新的月报批次，并记录脱敏阻塞原因。
- 月报预算快照固定为每个供应商最多 3 次搜索（首轮 2 次、证据不足时最多 1 次补充），不再沿用手动任务最多 5 次的预算；实际每月搜索需求按启用供应商数动态计算。
- 月报预检只校验和门禁，不提前虚构已发生的 Provider 调用；真实搜索仍由 Worker 在每次动作前原子预占并在结果返回后结算。
- 本切片只改月报调度前置门禁和预算快照，不开启月报开关，不启动 Scheduler，不执行真实搜索、网页读取、模型调用或 ECS 操作。

### 16.78 月报默认 100 家范围与全量切换开关（当前轮次）

- 不新增供应商核心字段；默认月报范围由 `RESEARCH_MONTHLY_SUPPLIER_SCOPE=limited` 和 `RESEARCH_MONTHLY_SUPPLIER_LIMIT=100` 控制，按启用供应商 `id` 升序稳定选择最多 100 家。
- `RESEARCH_MONTHLY_SUPPLIER_SCOPE=all` 保留全量 150 家配置开关，切换前必须重新通过 Provider 额度、月报硬上限和平台安全余量预检；默认不会因启用供应商数量增加而自动扩容。
- 预检、批次创建、容量阻塞恢复和批次快照统一使用同一范围选择函数，确保计算和实际扇出一致。
- 月报触发时先生成一次供应商列表快照，并将该列表同时交给预检和批次创建，避免全量模式下并发启停供应商造成数量漂移。
- 默认 100 家按每家最多 3 次搜索估算 300 次/月，结合手动预留 300 次和安全余量 250 次，在 1000 次额度下保留 150 次余量；全量 150 家估算 450 次/月，只有在明确接受额度压力或提升额度后切换。
- 已完成后端配置、Compose/生产示例、月报预检与批次扇出改造；回归覆盖 150 家启用供应商默认创建 100 个子任务，以及 `scope=all` 创建 150 个子任务。未增加供应商字段或数据库迁移，未启动真实 Provider、网页读取、模型调用或 ECS。

### 16.70 周期批次单 Worker 容量阻塞与恢复（当前轮次）

- 当同一周期类型的前一批次仍处于 `queued/running` 时，新周期不直接扇出子任务，而是创建 `capacity_blocked` 批次，保留供应商快照、预算快照和阻塞原因。
- Scheduler 每分钟检查可恢复的容量阻塞批次；前序批次进入终态且当前周期额度/开关预检再次通过后，才按原快照幂等创建子任务并转为 `queued`。
- 月报和周报共享同一恢复入口，仍由单 Worker 串行认领，不通过提高并发解决容量问题；关闭开关、额度不足或前序批次未完成时保持阻塞。
- 本切片不启动 Scheduler、不执行真实 Provider、网页读取、模型调用或 ECS 操作。
- 本地验收已完成：Alembic `0039 -> 0040` 成功并到达 `0040 (head)`，`research_batches.topic` 已落库；周期调度定向测试 `8 passed`，全后端 pytest `316 passed`，应用/测试 Ruff 和容器内 mypy（79 个源文件）均通过。
- 本地 `app` 已重建并替换为镜像摘要 `sha256:c1f5d330628deebfd102b90ed552a2454291ba676bf18f10c2b326f675c02eb9`，健康接口返回 `status=ok`、`database=ok`；Scheduler 保持停止，未执行真实 Provider、网页读取、模型调用或 ECS 操作。

### 16.71 周期子任务停用跳过与预算终态（当前轮次）

- 批次创建后若供应商在子任务执行前被停用或删除，周期子任务标记为 `skipped`，记录脱敏原因，不进入搜索、网页读取或模型调用。
- `ResearchBudgetExceeded` 不再伪装为普通 `failed`，任务标记为 `budget_exhausted`；批次分别统计跳过和预算耗尽数量，组合报告仍只聚合已完成且已回验的子报告。
- 批次聚合器接受 `skipped`、`budget_exhausted` 两类终态；含跳过/预算耗尽的批次不会被标记为全量成功，保留 `partial` 或 `failed` 语义。
- 本切片只验证状态与外部调用门禁，不启动 Scheduler，不执行真实 Provider、网页读取、模型调用或 ECS 操作。
- 本地验收已完成：Alembic `0040 -> 0041` 成功并到达 `0041 (head)`，`budget_exhausted_count` 已落库；定向测试 `19 passed`，全后端 pytest `319 passed`，应用/测试 Ruff 和容器内 mypy（79 个源文件）均通过。
- 本地 `app` 已替换为镜像摘要 `sha256:1db601bf2406140a30befa5ec39cf4de8ef1eaa28e4b8df65f8c2af5dbc0bed7`，健康接口返回 `status=ok`、`database=ok`；Scheduler 保持停止，未执行真实 Provider、网页读取、模型调用或 ECS 操作。

### 16.72 批次取消与预算耗尽批次状态（当前轮次）

- 批次状态补充 `budget_exhausted`，仅预算耗尽且没有成功子任务时使用；同时保留 `partial` 表达成功与失败/跳过/预算耗尽混合结果。
- 新增批次级取消接口：容量阻塞批次立即取消；排队子任务立即取消；运行中子任务只设置取消请求，等待 Worker 安全结束，避免并发启动下一批次。
- 批次取消、子任务取消请求和最终状态均写入审计/任务事件，重复取消保持幂等，不触发外部调用。
- 本切片只验证本地取消和状态聚合，不启动 Scheduler，不执行真实 Provider、网页读取、模型调用或 ECS 操作。
- 本地验收已完成：Alembic `0041 -> 0042` 成功并到达 `0042 (head)`；研究批次与 Worker 定向测试 `13 passed`，全后端 pytest `324 passed`，应用/测试 Ruff 和容器内 mypy（79 个源文件）均通过。
- 本地 `app` 已替换为镜像摘要 `sha256:5cc064db8c94eac67eecf48c52da6966b68b0b7f5c8a4967fce19b4a3d7cd452`，健康接口返回 `status=ok`、`database=ok`；Scheduler 保持停止，未执行真实 Provider、网页读取、模型调用或 ECS 操作。

### 16.73 Worker 协作式租约续期（当前轮次）

- 工具动作认领、预算预留和用量记账成功时，当前 Worker 将有效任务租约向后续期，避免多页面/多步骤研究在正常进度下被误判为失联并被重复认领。
- 续期仍要求任务处于 `running`、Worker 身份匹配且原租约未过期；过期租约不能自助复活，保持现有恢复和对账安全边界。
- 本切片不引入后台线程或新依赖；单个外部请求的最长耗时仍必须小于租约窗口，超过该边界需要后续独立的可中断执行器设计。
- 本切片只验证租约状态与工具账本边界，不启动 Scheduler，不执行真实 Provider、网页读取、模型调用或 ECS 操作。
- 本地验收已完成：租约、工具账本、预算和 Worker 定向测试 `20 passed`，全后端 pytest `326 passed`，应用/测试 Ruff 和容器内 mypy（79 个源文件）均通过。
- 本地 `app` 已替换为镜像摘要 `sha256:5054bf2a06a6ee58e474dff40d2c5e59e83b48ec33739aa5f22c6462a76a3ce1`，健康接口保持正常；Scheduler 保持停止，未执行真实 Provider、网页读取、模型调用或 ECS 操作。

### 16.74 LangGraph checkpoint 任务 provenance（当前轮次）

- `research_tasks` 补充 `graph_version` 和 `checkpoint_thread_id`，LangGraph 执行前保存当前图版本与实际线程键，任务查询可直接定位恢复上下文。
- legacy 执行器和历史任务保持字段为空；图版本变化时使用新线程键，不把旧 checkpoint 误交给新图结构。
- 本切片只补齐 provenance 和恢复可观测性，不改变工具账本、预算、租约、报告或外部调用语义。
- 本切片不启动 Scheduler，不执行真实 Provider、网页读取、模型调用或 ECS 操作。
- 本地验收已完成：新增迁移已将数据库推进到 `0043 (head)`，并确认两个字段已落库；研究 Runner 与 checkpoint 定向回归 `13 passed`，全后端 pytest `326 passed`，Ruff 通过，容器内 mypy（79 个源文件）通过。
- 本地 `app` 已使用镜像摘要 `sha256:0ee3e3845f26f65d97dea66aae1d268a4f9c68a5175175b9df40e82f17d4ae52` 重建并健康运行，健康接口返回 `{"status":"ok","database":"ok"}`；Scheduler 保持停止，未执行真实 Provider、网页读取、模型调用或 ECS 操作。

### 16.75 候选来源失败诊断与公开证据不足终态（当前轮次）

- 候选页面读取失败继续按单来源隔离，不因单个 403、robots、网络或 URL 校验错误中断同一研究任务；事件只记录脱敏的 `error_kind` 和可选 HTTP 状态码，不保存 URL、正文或原始异常。
- 每轮候选读取追加汇总事件，记录候选总数、成功读取数、失败数和重复数，供研究执行图和审计回放解释“搜索完成但没有证据”的原因。
- legacy 与 LangGraph 两条本地执行路径在没有任何已回验证据时均生成“公开证据不足”草稿并正常结束任务；不调用模型、不写入 `raw_signals`，也不把证据不足伪装为风险结论。
- 新增 Runner 回归覆盖两条编排路径、`SourceRequestFailed` 分类、状态码脱敏、候选失败汇总和不足证据报告；定向研究 Runner 测试 `13 passed`，Ruff、mypy 和 Python 语法检查通过。
- 本切片仅修改本地后端诊断与终态语义，不启动 Scheduler、Worker 常驻进程、真实 Provider、网页读取、模型调用或 ECS。

### 16.76 宏切片推进方式与 M1 本地闭环稳定化（2026-08-17）

- 为避免研究轨继续被拆成大量用户可见的微切片，后续按四个功能包验收：M1 手动受控研究 Agent 本地闭环、M2 月报批次（默认最多 100 家，保留全量开关）、M3 本地集成与可测试交付、M4 ECS部署验证阶段0；原 15.1～15.16 和 16.60～16.75 仅保留为历史实施记录。
- 本轮进入 M1，目标是让“创建任务 → 显式开始 → Worker 认领 → LangGraph 受控执行”具备可重复的本地启动路径；不把当前固定图表述为通用 Agent，不扩大真实联网、模型或付费 Provider 授权。
- 新增 `scripts/start-research-worker.ps1`：默认以 `topic_source_discovery + langgraph` 启动 `research-local-test` profile；也支持 `local_lifecycle_test`、`controlled_single_page` 和 `legacy` 回滚参数。脚本只向 Compose 子进程注入开关，不打印 `.env`、Key、Token 或密码。
- `.env.example`、Compose 注释和 README 明确区分 app/Scheduler 与 research-worker：前者不会自动执行研究任务；未显式启动 Worker 时，任务保持 `queued/running` 等待认领是预期状态。
- M1 验收以启动配置可审计、Worker 能认领已显式开始任务、停止 Worker 后不产生新外部调用为准；不启动 Scheduler 常驻、不执行真实搜索、网页读取、模型调用或 ECS 操作。
- 本轮已完成 PowerShell 脚本语法检查、`docker compose --profile research-local-test config --quiet` 配置检查，并在 Compose 网络内通过研究 Runner/Worker 定向 Fake/Mock 回归；未发生真实 Provider、网页读取或模型调用。
- M2～M4 的批次、额度、前端集成、镜像和 ECS 门禁保持独立，不因本轮本地启动脚本完成而提前宣称研究轨或 ECS 已完成。

### 16.79 M2 范围快照加固与 M3 本地镜像验收（2026-08-17）

- 月报触发时先生成一次范围供应商列表，同时交给额度预检和批次扇出；默认 `limited` 稳定选择最多 100 家，`all` 选择全部启用供应商，避免并发启停造成预检/执行数量漂移。
- Docker 应用环境月报调度回归通过；本机 `D:\DevTools\SupplierRiskMonitoring\.venv` 的 Ruff、mypy、Python 编译检查和无数据库搜索 Provider 测试通过。
- 前端 Vitest `6/6`、TypeScript 检查、Vite 生产构建通过；Compose 配置检查通过。生产构建仍有既有主 JS 包超过 500 kB 的非阻断提示。
- `supplierriskmonitoring-app:latest` 已重建并在新镜像内复跑月报调度测试；本轮未重启现有 app、Scheduler、Worker 或数据库，未执行真实 Provider、网页读取、模型调用或 ECS 操作。

### 16.80 M3 app/Worker 本地启动联调（2026-08-17）

- 新镜像已替换本地 app 容器，容器健康检查通过；`GET /api/v1/system/health` 返回 HTTP 200，数据库状态为 `ok`。
- 使用 `scripts/start-research-worker.ps1 -Mode local_lifecycle_test -Orchestrator legacy -Detached` 启动临时 Worker，容器正常进入运行态并完成迁移初始化；随后主动停止，未认领或执行研究任务。
- 数据库最近心跳记录为 `local_lifecycle_test|legacy|stopped`，证明 Worker 注册与优雅停止链路可用；Scheduler 保持停止，RSSHub/Crawl4AI 未在本轮启动。
- 当前本地 app 镜像标识为 `sha256:0e61f3381b6e64723d36a1f29f978cbdf31ad1fef66307b3850daf46e3195b0f`；本轮仅完成 app/Worker 后端联调，不能据此宣称 M3 整体完成。

### 16.81 M3 localhost 前端实景交付（进行中，2026-08-17）

- 研究轨前端已正式接入主应用：支持手动创建任务、选择监控轨供应商范围、显式开始/取消、任务状态动态刷新、真实执行事件图（桌面网络图/窄屏时间线）、监控轨与公开网页证据来源、引用报告草稿和失败/空状态。
- 研究轨页面新增 Worker 状态提示，直接读取 `/api/v1/research/worker/status`，区分 `online`、`stale` 和 `offline`；Worker 未启动时显示本地启动命令，避免把“等待 Worker”误解为前端无响应。
- 已完成前端 Vitest `6/6`、TypeScript 检查和 Vite 生产构建；构建产物已同步到 `backend/app/static`，localhost 不再使用旧研究轨之前的静态包。
- M3 尚未完成：仍需在有效登录会话下由用户手动访问 localhost，完成桌面/窄屏页面、创建任务、开始任务、执行图和失败状态的实景验收；验收通过后再关闭本节点。ECS部署验证阶段0不变。

### 16.77 M1 Worker 心跳与等待状态诊断（2026-08-17）

- 新增 `research_worker_heartbeats` 加法迁移 `0044`，Worker 启动、运行轮询和优雅停止分别写入注册、续期和停止状态；异常退出不猜测恢复，API 按 `last_seen_at` 将记录标记为 `stale`。
- 新增受权限保护的 `GET /api/v1/research/worker/status`，返回 `online`、`stale` 或 `offline` 以及模式、编排器和最近心跳；不返回密钥、Provider 响应或任务正文。
- 研究任务执行语义不变：没有 Worker 时任务仍等待认领；Worker 在线但尚未认领时可通过状态接口区分；优雅停止后不再认领新任务。
- 本地验收已通过 0044 迁移、Worker 心跳生命周期与状态 API 定向测试、研究 Runner/Worker 回归、Ruff 和 mypy；未启动常驻 Worker，未执行真实 Provider、网页读取、模型调用或 ECS。

### 16.82 M3 研究轨区级匹配、网页回退与执行图增量布局（进行中，2026-08-17）

- 监控证据进入研究轨前增加区县级二次校验：兼容历史供应商把“松江区”等值存放在 `city` 的情况；当事件名称携带“宝山区/闵行区”等区县而 `district` 为空时，不再退回到“上海市”级宽松匹配。旧的跨区气象提醒会被过滤，并在事件流记录 `reason=district_mismatch`，不会进入研究证据或报告引用。
- 公网单页读取器补齐 `SourceFetchError` 分类，并增加空正文触发的 Crawl4AI 单页回退。回退仅允许网络错误、上游错误和空正文；403/WAF、限流、认证、robots、重定向和超大响应不通过浏览器绕过。Crawl4AI 配置默认关闭，启用时由受控 Token 和 `internal` 网络提供，响应仍经过正文、最终地址和状态校验。
- Compose 将 app/research-worker 同时接入默认网络和 `internal` 网络，使本地旁路 Crawl4AI 在同一 Compose 项目中可被 Worker 访问；生产/旁路仍需独立通过 ECS部署验证阶段0的镜像、漏洞、资源和出网门禁后才能开启。
- 研究执行图取消公网候选固定高度和内部纵向滚动；候选来源按事件流顺序在轨道内自然向下排列，证据池改为轨道汇聚线，避免每个候选直接连证据池造成线束纠缠。前端轮询间隔调整为约 900ms，终态会补拉剩余事件；新增候选节点使用基于事件序号的渐进动画，不伪造后端状态。
- 活动任务的事件接口增加小批量增量读取：执行中首轮每次只拉 1 条、轮询每次最多 2 条，终态再补拉剩余事件；历史终态任务仍完整加载，确保回放完整与实时生成兼顾。
- 失败节点详情增加 `error_kind`、HTTP 状态和过滤原因的中文解释，避免所有候选只显示“读取失败”。
- `scripts/start-research-worker.ps1` 增加显式 `-EnableCrawl4AI` 开关；它要求已准备被 Git 忽略的 `deploy/.env.stage0`，不自动生成或打印 Token。不开关时保持纯受控直连，避免误启浏览器出网。
- 本地验证：研究网页读取回归 `11 passed`；Compose 网络内研究 Runner 与风险匹配定向回归 `25 passed`（含跨区监控证据过滤）；受影响后端 Ruff、mypy 通过；前端 Vitest `6 passed`、TypeScript 检查和 Vite 生产构建通过；Compose 配置渲染通过。宿主 PowerShell 直跑数据库测试仍会因不在 Compose 网络内而无法解析 `postgres`，不作为代码失败结论。
- M3 仍未完成：需在 localhost 有效登录会话下由用户手动验收区级气象过滤、候选失败分类、动态执行图和 Crawl4AI 回退；验收前不关闭本节点，也不宣称 ECS 或公网全网抓取能力已上线。

### 16.83 研究任务删除与历史数据清理（2026-08-17）

- 新增研究任务删除接口：沿用任务所有权/管理员权限，运行中的任务返回冲突，排队和终态任务级联清理来源、引用、结论、报告、事件和工具调用账本，并记录 `research_task_deleted` 审计事件。
- 研究轨前端任务列表每行增加删除按钮与二次确认；删除当前任务后自动选择下一条，避免详情面板残留已删除数据。
- 本地已按精确 ID 清理 12 条旧/测试终态任务，删除前确认 `queued/running=0`，删除后研究任务及其子数据均为 0 条；供应商、研究批次配置未受影响。
- 删除能力已进入 M3 本地交付范围；M3 仍需有效登录会话下的 localhost 实景验收，ECS部署验证阶段0不变。
