# 《智能信源采集与分析落地执行计划》评估意见

> 评估日期：2026-08-11  
> 评估对象：[智能信源采集与分析落地执行计划.md](./智能信源采集与分析落地执行计划.md)  
> 版本说明：第 1～8 节评估 v1.0；第 9 节复评 v1.1 并形成条件审批意见  
> 评估性质：架构与实施可行性评估；批准范围以第 9 节及当前 v1.2 批准稿为准

## 1. 总体结论

这份计划的技术方向可行，但当前不建议直接进入开发。

分开评价如下：

| 评价对象 | 结论 |
|---|---|
| 固定 RSS/网页信源采集扩展 | 方向可行，修改后可执行，当前约 6/10 |
| 通用联网研究 Agent | 目标明显偏离，当前约 3/10 |
| 2 核 4G ECS 持续运行全部组件 | 不应未经实测承诺 |
| n8n 作为第二阶段编排中心 | 当前过早，暂不建议引入 |

根本问题不是 RSSHub 或 Crawl4AI 能否运行，而是本计划最终仍然建设了“更多固定数据源进入现有风险评分链”，没有实现此前确定的“自主搜索、多信源交叉验证、生成带引用研究报告”的产品方向。

建议先将本计划退回修订，明确选择以下两种定位之一：

1. 如果只扩大固定信源采集能力，将文档改名为《RSS 与网页信源采集扩展执行计划》；
2. 如果落实新的平台定位，则增加独立的研究任务、引用和报告链路，不能让所有网络内容默认进入 `raw_signals` 和风险评分链。

## 2. 与产品定位的偏差

原计划的闭环是：

```text
配置信源
  → RSSHub / Crawl4AI
  → raw_signals
  → 现有 AI 解析
  → 风险事件、供应商关联与评分
  → 风险提醒
```

这适合“固定信源采集扩展”，但不等于通用情报研究 Agent。计划没有覆盖以下能力：

- 自主生成搜索查询；
- 从预置清单之外发现候选信源；
- 多信源交叉验证；
- 来源可信度分层与冲突处理；
- 事实、推断和预测分离；
- 带逐条引用的研究报告；
- 日报、周报和专题研究任务；
- 手动即时研究任务；
- 将研究结论选择性转为正式风险信号。

因此建议采用双轨架构，而不是把两类数据混成一条链：

```text
确定性监控轨
天气、地震、官方清单
  → raw_signals
  → 现有 AI 解析
  → 本地匹配与评分
  → 风险提醒

情报研究轨
搜索服务
  → 网页读取
  → 去重、来源分级、交叉验证
  → 研究 Agent
  → research_reports + citations
  → 日报、周报、手动报告
```

只有在研究任务明确形成可监控事件时，才允许将结果转成 `raw_signals`；研究报告本身不应被强制转换为 P1-P4 风险提醒。

## 3. P0 阻断问题

### 3.1 LLM 分析链会重复调用

原计划拟在写入 `raw_signals` 前增加一次模型调用，完成清洗、分类、摘要、关键词和供应商候选识别，然后再进入现有风险链。

当前系统已经具备一次完整的信号 AI 解析能力，包括：

- 风险大类和细类；
- 主体、地点、产品和行业提取；
- 中文摘要；
- 证据句；
- 置信度；
- JSON Schema 校验和重试。

如果直接按原计划实现，将变成：

```text
第一次 LLM 分析
  → raw_signals
  → 第二次 LLM 分析
  → 风险事件与评分
```

这会增加 Token 成本、结果漂移和审计复杂度。

应改为：

```text
抓取
  → 确定性清洗
  → 稳定指纹去重
  → raw_signals
  → 相关性预过滤
  → 复用现有一次 LLM 解析
  → 风险链
```

LLM 不应参与基础指纹生成或确定性正文去重，否则模型改写可能造成同一内容出现不同指纹。

### 3.2 旁路容器会绕过实际目标域名治理

原计划把 RSSHub 和 Crawl4AI 放在内网，只允许 FastAPI 调用。这个做法只能保护服务入口，不能自动覆盖旁路容器对外发出的真实请求：

```text
平台 → RSSHub 内部地址 → 外部目标网站
平台 → Crawl4AI 内部地址 → 外部目标网站
```

当前 `source_host_access` 的域名租约、最小间隔、403/WAF 熔断、429 冷却和网络错误退避，是围绕平台发出的目标 URL 建立的。若实际外网请求由旁路容器完成，平台可能只看到内部服务地址，无法准确治理最终目标域名。

必须补充：

- 平台在创建任务时验证最终目标 URL；
- Crawl4AI 自身启用 SSRF、重定向逐跳校验和 DNS 重绑定保护；
- RSSHub 只允许登记过的路由模板，不允许任意路由参数；
- 每次任务记录真实目标主机；
- 旁路服务自身实现目标域名级并发、间隔和冷却；
- 出网防火墙禁止访问云元数据、私网、回环地址和宿主机管理端口。

原计划中“代理轮换”的表述也应删除。当前项目边界不允许使用代理轮换绕过 WAF 或封禁；只能停用该源，或评估授权商业数据服务和单一合规出口代理。

### 3.3 2 核 4G 资源预算不足以直接承诺

原计划只估算新增 RSSHub、Crawl4AI 和 n8n 的内存，没有把以下基础服务计入整机峰值：

- PostgreSQL/PostGIS；
- FastAPI；
- Scheduler；
- Nginx；
- Docker 和操作系统；
- 浏览器瞬时峰值；
- 备份、迁移和日志开销。

Crawl4AI 本身包含浏览器运行时，动态页面、并发任务和页面崩溃恢复都可能产生峰值。将其设置为 1536MB 上限并不能证明 2 核 4G 足够，反而可能造成 OOM 重启循环。

建议容量边界：

| 部署形态 | 判断 |
|---|---|
| 2 核 4G，仅做低并发阶段一 POC | 可以尝试，但必须单浏览器、单研究任务并实测 |
| RSSHub + Crawl4AI 与现有平台长期共存 | 建议 4 核 8G |
| 再叠加 n8n、多用户和浏览器研究 | 建议拆分实例或至少 4 核 8G |

验收不应只写“新增服务内存不超过 1.8GB/2.3GB”，而应测量整机：

- 基础服务实际内存基线；
- 峰值时保留至少 25%～30% 可用内存；
- 浏览器并发固定为 1；
- 连续 48 小时 OOM 次数为 0；
- 任务积压能熔断；
- 重启后任务可恢复且不重复调用模型。

## 4. 重要架构问题

### 4.1 n8n 当前属于过早引入

当前 Scheduler 已支持每个数据源独立 cron、调度注册表刷新、采集、AI 解析、规则处理和保留清理。此时再引入 n8n，会形成两个编排中心，增加：

- 重复调度；
- 重复重试；
- 模型调用预算分散；
- 提示词和 Schema 双版本；
- 密钥同时存在于平台和 n8n；
- 数据库事务边界不清晰。

特别不建议让 n8n 工作流直接调用 DashScope。模型调用应集中在 FastAPI 后端，继续使用现有 Provider 抽象、预算、Schema 和审计机制。

n8n 只有在出现复杂跨系统 DAG、审批、通知和非技术运营人员需要维护工作流时，才有明确引入价值。当前应将其降为阶段三候选，不作为阶段二承诺。

### 4.2 `source_type` 与采集方式不应混用

计划提出给 `data_sources.type` 增加 `rsshub_route` 和 `crawl_page`。当前实际字段是 `source_type`，而且它表达的是信源或风险语义，不是传输方式。

建议分开：

- `source_type`：weather、policy、industry、sanctions 等业务语义；
- `connector_kind`：builtin、declarative_json、rss、rsshub、crawl4ai；
- `adapter_config`：受 Schema 校验的连接器参数。

否则后续页面筛选、风险维度统计和规则配置会混淆“信源是什么”和“信源怎么抓取”。增加几个文本值本身也不应成为单独迁移的理由，除非同时增加约束、索引或行为。

### 4.3 Crawl4AI 不是“给一个 URL 就稳定完成接入”

Crawl4AI 可以将网页渲染并转成适合模型的正文，但列表页与详情页仍需要配置：

- 种子 URL；
- 允许域名；
- 最大深度和页面数；
- 链接包含/排除规则；
- 内容大小上限；
- JavaScript 是否允许；
- 页面超时和任务预算。

因此“免选择器配置”只能表示不要求传统 CSS 选择器，不表示无需抓取策略。当前只保存一个页面 URL 的信源模板不足以保证稳定采集。

### 4.4 RSSHub 路由不等于官方授权

“RSSHub 有路由”只表示 RSSHub 项目提供了一个路由实现，不代表目标网站官方授权自动采集。每个路由仍应记录：

- 原网站和实际目标域名；
- 路由维护状态；
- 原网站数据条款；
- robots 要求；
- 是否允许服务器自动采集；
- 是否允许保存正文和内部再利用。

公开可访问不等于允许批量自动采集。

### 4.5 不能使用 `latest` 镜像

生产计划中的 `diygod/rsshub:latest` 和 `unclecode/crawl4ai:latest` 不满足可复现部署和供应链安全要求。

应固定：

- 版本号；
- 镜像摘要；
- Node/Python 运行时版本；
- 浏览器版本；
- 升级前的回归验收记录。

抓取服务还应默认不对公网暴露，并且不能接受任意 JavaScript、任意脚本或任意浏览器配置。

## 5. 成本与模型配置问题

计划按 50 个信源、每天每源 20 条估算每天 1000 次模型调用、每月约 30000 次调用。这个估算尚未计入：

- 抓取失败重试；
- 长网页截断；
- 低相关性过滤前的输入；
- 周报和研究报告生成；
- 模型输出校验失败重试；
- 研究 Agent 多步工具调用。

因此“月成本数十元”不应写成确定结论，应改为计算公式，并在开发前用当日 DashScope 价格验证。

必须增加：

- 单信源每日条目上限；
- 全局每日 Token 和金额上限；
- 单正文最大字符数；
- LLM 前确定性相关性过滤；
- 超预算自动停止新分析；
- 管理员立即停用 Scheduler 和手动 Agent 的止损开关。

`qwen3.7-max` 也应先确认实际可用的模型 ID、上下文上限和计费，不应写死在架构文档中。现有平台已经使用 `AI_PROVIDER`、`AI_BASE_URL`、`AI_MODEL` 和 `AI_API_KEY` 抽象，不建议再平行新增一套 `DASHSCOPE_API_KEY` 配置。

## 6. 修订后的实施建议

### 阶段 0：技术 Spike 与容量实测

只验证三个连接器：

1. 一个原生 RSS；
2. 一个 RSSHub 路由；
3. 一个 Crawl4AI 页面。

测试内容：

- 2 核 4G 峰值内存和 CPU；
- 页面正文长度与 Token 数；
- robots、SSRF、重定向、403、429 和超大响应；
- 失败恢复、重复抓取和容器重启；
- 单源域名治理是否真的落在最终目标主机。

### 阶段 1：固定信源采集扩展

只新增：

- 原生 RSS 连接器；
- RSSHub 连接器；
- Crawl4AI 连接器；
- 连接器配置 Schema；
- 复用现有指纹、`raw_signals`、AI 解析和 Scheduler；
- 采集运行和来源健康状态。

阶段一不新增第二套 LLM 分析器，不引入 n8n，不让 Scheduler 内部采集额外绕过现有的域名访问治理。

### 阶段 2：通用研究 Agent

如果要实现新的平台定位，应增加独立的研究模型：

- `research_tasks`：手动、每日、每周研究任务；
- `research_sources`：实际检索到的来源；
- `research_citations`：URL、标题、发布时间、访问时间、正文摘录和摘录位置；
- `research_reports`：报告正文、结论类型、模型版本和生成状态。

研究任务应支持：

- 搜索提供商抽象；
- Crawl4AI 或其他网页读取服务；
- 多来源交叉验证；
- 事实、推断、预测分段；
- 引用与结论绑定；
- 日报、周报和手动即时报告；
- 仅在满足事件条件时转入风险监控链。

### 阶段 3：是否引入 n8n

根据阶段一和阶段二的真实任务复杂度再决定。不要因为“可视化编排”本身提前引入第二套调度系统。

## 7. 建议补充的验收标准

除原计划中的接入和资源验收外，至少补充：

1. 同一原文重复采集不会产生重复 `raw_signals`，也不会重复调用 LLM；
2. 旁路容器的真实目标域名可被平台记录、限速和冷却；
3. 被 robots、403、429 或 WAF 拒绝时，目标站点不会被继续重试；
4. 抓取服务不接受任意脚本、任意私网 URL 或任意浏览器会话；
5. 研究报告每个关键结论都有对应引用，引用不存在或正文不支持结论时报告失败或降级；
6. 研究报告明确区分事实、推断和预测；
7. 单日 Token、金额、抓取页数和并发都有硬上限；
8. 超预算后自动停止新增分析，管理员能够立即暂停 Scheduler 和手动调用；
9. 2 核 4G POC 连续 48 小时无 OOM、无任务重复和无未处理积压；
10. 镜像和模型版本固定，升级有回滚记录。

## 8. 最终审批意见

当前建议状态：

> **有条件退回修改，不批准按 v1.0 直接启动开发。**

保留以下内容：

- 原生 RSS 优先；
- RSSHub 和网页抓取作为可选连接器；
- 默认停用和管理员确认门；
- 密钥不进入信源配置、日志和模型上下文；
- 48 小时连续运行验证；
- 失败后停源或降级为只保存原文。

优先修正以下五项：

1. 明确固定信源扩展与通用研究 Agent 的产品边界；
2. 删除重复 LLM 分析链；
3. 解决旁路容器绕过真实目标域名治理的问题；
4. 按整机而不是新增容器重新计算资源；
5. 暂缓 n8n，增加独立研究任务、引用和报告模型。

本评估只针对架构和执行计划，不代表第三方组件已经安装、联调或获得生产授权。

## 9. v1.1 复评与条件审批（2026-08-11）

v1.1 已纠正 v1.0 的主要方向偏差：明确监控轨与研究轨隔离，删除重复 LLM 分析设想，暂缓 n8n，并增加研究任务、来源、引用和报告模型。架构方向可以通过，但仍有六项在开工前必须固化：

1. 长研究任务必须由独立异步 Worker 执行，FastAPI 请求和现有短对话 ReAct 循环不得承载完整研究流程。
2. PostgreSQL 任务队列必须具备原子认领、租约、心跳、取消、重启恢复和幂等，避免重复执行与重复计费。
3. 研究结论与引用必须建立逐条结构化关系；同一结论转为风险信号必须幂等且保留审计。
4. RSSHub/Crawl4AI 旁路必须遵守平台最终目标域名治理，并返回最终 URL、重定向链和 HTTP 状态供审计。
5. 补齐四角色 RBAC、任务所有权和供应商范围隔离、搜索查询最小化、独立留存策略及单任务 Token/金额硬上限。
6. 2 核 4G 只可用于阶段 0 低并发验证；固定组件版本并提交 SBOM/漏洞扫描，48 小时浸泡后无 25%～30% 余量则升级 4 核 8G 或拆分。

复评审批结论：

> **架构方向通过；仅批准阶段 0 技术 Spike；阶段 1 和阶段 2 需分别再次审批。**

阶段 0 的主动实施预计 2～3 个工作日，另需 48 小时浸泡，因此日历排期至少 4～5 天。批准内容仅限固定版本组件试跑、搜索 Provider 实测、单页读取、旁路治理、异步任务与引用闭环、成本及容量验证；不包括正式发布、批量采集、生产凭据录入、自动日/周研究或对外抓取服务。

上述六项已纳入 v1.2 阶段 0 批准稿，但是否实际满足必须以阶段 0 的代码、测试、运行记录、SBOM 和资源监控证据验收，不能以文档完成替代技术验收。
