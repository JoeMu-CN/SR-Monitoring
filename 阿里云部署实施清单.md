# 供应商风险监控平台 · 阿里云部署与 AI 增强实施清单

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.1（新增交互式查询与邮件推送模块） |
| 编制日期 | 2026-08-07 |
| 前提 | MVP（D1~D10）已按技术方案验收 |
| 目标 | 迁移至阿里云 ECS，接入千问 API + 天眼查 MCP，实现 7×24 智能监控 |

---

## 阶段 0：准备与授权确认（开工前，约 2~3 天）

### 0.1 账号与权限

- [ ] 注册/实名认证阿里云企业账号（与天眼查企业主体一致，便于后续发票和备案）
- [ ] 开通阿里云百炼平台，记录 API-KEY（控制台 → API-KEY 管理）
- [ ] 确认百炼新人免费额度已到账（每模型 100 万 tokens，90~180 天有效）
- [ ] 开启百炼"免费额度用完即停"开关，防止意外扣费
- [ ] 创建阿里云 RAM 子账号用于日常运维，主账号开启 MFA

### 0.2 天眼查授权核查（关键路径，最先启动）

- [ ] 调取天眼查企业付费会员合同，确认是否包含：API 接口额度 / MCP 调用权限 / 自动化调用授权 / 数据留存期限
- [ ] 若合同不含接口额度，联系天眼查商务（400-608-0000）询价：企业版 API 套餐或按次充值包
- [ ] 取得书面授权前，开发阶段仅使用个人账号免费 MCP 额度（100 次/天、1000 次/月），并在代码中写明该数据源为"开发专用"
- [ ] 风险预案：若授权受阻，按技术方案 7.2 节降级为"人工导出文件 + manual-json 导入"模式

### 0.3 访问策略决策

- [ ] 确认过渡期方案：**纯 IP + 非标准端口 + 安全组 IP 白名单**（无需 ICP 备案）
- [ ] 收集需访问系统的公司出口公网 IP 列表
- [ ] 同步启动域名购买和 ICP 备案（周期 1~3 周，不阻塞过渡期内测）
- [ ] 确定邮件通知方案：优先复用公司企业邮箱 SMTP（零成本）；大量外发时再评估阿里云邮件推送 DirectMail
- [ ] 申请邮箱 SMTP 授权码，确定发送账号（如 risk-monitor@公司域名）
- [ ] 确定接收人清单与分级订阅规则（P1 实时推送全员、P2 推送采购负责人、日报发管理层）
- [ ] 确定交互查询的使用角色（运营/采购/管理层）和各自的查询权限边界

---

## 阶段 1：代码改造（约 3~5 天）

> 对应技术方案 12.2 节"后续云服务器迁移"清单。

### 1.1 安全加固（公网暴露前的强制项）

- [x] 实现用户认证：平台本地账号 + bcrypt 密码 + 服务端 Session，固定四角色权限矩阵
- [x] 密码哈希存储（bcrypt），禁止明文
- [x] 登录接口限流（5 次/5 分钟/来源键）防爆破
- [x] 审计日志脱敏复核：确认 Authorization、Cookie、API Key 不落日志
- [x] `compose.prod.yaml` 确认 PostgreSQL 和 app 不暴露宿主端口，仅由 Nginx 入口代理

### 1.2 千问接入（零业务代码改动）

- [ ] 生产 `.env` 配置：
  ```dotenv
  AI_PROVIDER=openai-compatible
  AI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
  AI_MODEL=qwen-plus
  AI_API_KEY=<百炼API-KEY>
  AI_TIMEOUT_SECONDS=90
  AI_MAX_RETRIES=2
  ```
- [ ] 用 5~8 条验收样例信号验证 qwen-plus 结构化输出通过率
- [ ] 若 Pydantic 校验失败率偏高，备选 qwen-max 对比测试（成本约 10 倍，仍属低额）
- [ ] 保留 FakeAIProvider 用于自动化测试

### 1.3 天眼查 MCP 数据源适配器

> ✅ 2026-08-07 状态更新：调用方式已确认 = **天眼查 AI 智能体平台（ai.tianyancha.com）MCP 网关 + `tyc_` 开头 API Key**（免费注册）。端点 `https://mcp.tianyancha.com/v1`（Streamable HTTP / JSON-RPC 2.0），鉴权头 `Authorization: <Key>`。工具面：`search_companies` 锚定 → `get_company_basic_profile` / `get_company_capabilities` → `call_tool`。计费口径：成功且返回有效结果计 1 次，报错/无结果/系统异常计 0 次。
> ✅ 预算控制器已实现（`backend/app/agent/budget.py` + `tyc_usage_records` 表 + 5 项测试）。
> ✅ 真实网关已实现（`backend/app/agent/tyc_gateway.py`）：手写 Streamable HTTP 客户端（零新依赖），initialize 握手 + tools/call search_companies，兼容 SSE/JSON 响应，鉴权失败/额度超限/网络错误分类处理；6 项协议级测试通过（MockTransport 模拟）。

- [x] 新增 `TYCSourceAdapter`，实现 SourceAdapter 四接口（fetch / normalize / fingerprint / healthcheck）
- [x] **调用预算控制器**：每日/每月计数持久化到 `tyc_usage_records`，默认 80 次/天、900 次/月，超限自动拒绝并返回 `quota_exhausted`，不调用网关
- [x] 真实 `TycGateway` 实现（`McpTycGateway`），`verify_company` 已接入
- [x] **MCP 端点、运行密钥、每日/每月额度统一在数据源控制台配置与启停**（迁移 0019、0021）：API Key 加密存库（`api_key_encrypted`，Fernet，密钥来自 `DATA_SOURCE_SECRET_KEY`），前端「数据源与同步状态 → 天眼查 → 编辑/启用」；生产环境不读取天眼查 `.env` 配置
- [ ] **上线前在数据源控制台录入天眼查 API Key 并启用**，用真实 Key 联调一次，确认 MCP 端点实际响应格式与解析器兼容；后续按需扩展司法/风险明细维度（capabilities → call_tool）

### 1.4 部署形态调整

> ✅ 2026-08-07 状态更新（D9）：独立 Scheduler 已实现（`backend/app/scheduler/`，
> APScheduler 容器，compose.yaml 已含 `scheduler` 服务）；手动触发 `POST /sources/{id}/run`
> 已实现；保留清理任务已实现（信号/AI 分析 90 天、事件与提醒失效后 90 天、采集运行 30 天，
> `RETENTION_*_DAYS` 可配置）。上云仅需编排调整。

- [x] compose 生产版新增 `scheduler` 独立容器（APScheduler 进程，对应技术方案 5.1）
- [x] 新增 Nginx 容器：反向代理 8080、安全响应头、gzip、请求体大小限制（`compose.prod.yaml` + `deploy/nginx/nginx.conf`）
- [ ] Nginx 监听生产 HTTPS 端口，配合安全组白名单（证书和端口需在目标 ECS 配置）
- [x] 数据保留清理任务按 3.3 节配置启用（90 天/30 天轮转，`RETENTION_*_DAYS`）
- [x] 新增数据库备份脚本（`deploy/backup-postgres.sh`；保留策略和 OSS 同步仍需在目标 ECS 配置）

### 1.5 交互式风险查询模块（Agent 对话）

> 超出 MVP 范围的新模块（技术方案 3.2 未含），作为上云后 v1.1 功能；与"提醒自动核查"共用同一 Agent 内核和工具集。

- [ ] 新增 `backend/app/agent/` 模块：ReAct 循环 + 工具白名单（业务库只读查询、天眼查 MCP、千问），复用现有 AIProvider 抽象
- [ ] 新增 `POST /api/v1/chat` 接口：会话管理、多轮上下文、工具调用日志、全量审计记录
- [ ] 支持四类查询场景：
  - 清单内风险问答（地点/产品/等级组合的自然语言查询）
  - 清单外企业临时核查（调天眼查一次性扫描，附"加入重点监控"确认按钮）
  - 假设性影响推演（如"台风影响宁波港波及哪些供应商"，走 PostGIS 空间查询）
  - 多轮追问（会话上下文保持）
- [ ] 前端新增聊天面板组件（Vue 3 + Element Plus），回答中嵌入 P 等级标签、证据链入口和天眼查额度实时余量
- [ ] 安全边界：Agent 对业务库只读；任何写操作（加入监控、启停供应商）必须由用户点击确认后走既有 API；回答必须附证据来源，无依据不下结论
- [ ] 天眼查临时核查调用计入统一预算控制器，对话中实时显示当日剩余额度

### 1.6 邮件通知模块（定期报告 + 实时高风险推送）

> 技术方案 3.2 将外部通知列为 MVP 不含范围，本节为 v1.1 新增；不引入 Celery/Redis，复用 APScheduler 与数据库队列。

- [ ] 新增 `EmailNotifier` 组件：SMTP 发送（公司企业邮箱）、连接池、失败重试、发送记录落库（收件人、主题、状态、关联提醒 ID）
- [ ] **定期风险报告**：
  - 日报：每日固定时间（如 08:30）汇总过去 24h 新增提醒、等级分布、重点变化，由 Agent 生成管理层可读摘要
  - 周报：每周一汇总趋势、数据源健康度、AI/天眼查额度消耗
  - 报告生成复用 Agent 摘要能力；生成失败时降级为纯数据模板，不错过发送
- [ ] **实时高风险推送**：
  - 触发条件：新生成 P1 提醒（可配置是否含 P2）
  - 防邮件风暴：同一供应商同一事件只推一次；多事件 15 分钟合并窗口；单小时上限（如 20 封）超限转合并摘要
  - 邮件内容：等级、供应商、事件摘要、匹配理由、证据链链接（直达系统详情页）
- [ ] 订阅配置表：接收人、订阅等级、日报/周报开关、免打扰时段；页面可维护
- [ ] 邮件模板：HTML + 纯文本双格式；敏感信息（密钥、内部编码规则）不出现在邮件中
- [ ] 演练：发送失败告警（SMTP 异常时页面提示）、退信处理

---

## 阶段 2：云资源开通（约 0.5 天）

- [ ] 购买 ECS：推荐 u1 实例 2核4G / 5M 带宽 / 80G ESSD（活动价 ¥199/年，续费同价）；若预算宽松选 4核8G（约 ¥955~1,600/年）
- [ ] 地域选择：距公司最近的大陆地域（如华东1 杭州 / 华北2 北京）
- [ ] 安全组规则：
  - 放行 22（SSH，仅公司 IP）
  - 放行 8443（Web，仅公司 IP 白名单）
  - 拒绝其余所有入站
- [ ] 创建 OSS Bucket（备份用，标准存储，预计 ¥50~100/年）
- [ ] 开通云监控免费告警：CPU > 85%、内存 > 85%、磁盘 > 80% 时短信/邮件通知
- [ ] 记录 ECS 弹性公网 IP（过渡期访问入口）

---

## 阶段 3：部署上线（约 1 天）

- [ ] ECS 初始化：安装 Docker、Docker Compose、fail2ban；创建非 root 部署用户
- [ ] 代码上传：git clone 或 rsync（确认 `.env` 不在版本库中）
- [ ] 编写生产 `.env`（仅放数据库、认证和 AI 配置；天眼查 MCP 端点、运行密钥和启停在数据源控制台配置，不写入生产 `.env`）
- [ ] `docker compose --env-file deploy/.env.production -f compose.yaml -f compose.prod.yaml up -d --build` 启动
- [ ] 验证：`/api/v1/system/health`、前端页面、登录墙生效
- [ ] 导入供应商 Excel 主数据（10~20 家试点 → 全量 100 家）
- [ ] 执行数据库首次备份并做一次恢复演练
- [ ] 配置 ECS 自动快照策略（每日，保留 7 天）

---

## 阶段 4：联调与试运行（约 5 天）

- [ ] 天眼查 MCP 全链路联调：建档核查 → 信号 → AI 解析 → 匹配 → 评分 → 提醒
- [ ] 核对调用计数器与天眼查控制台实际消耗一致
- [ ] 千问成本对账：百炼控制台用量统计 vs 本地 ai_analysis_records 记录数
- [ ] 演练降级场景：天眼查额度耗尽时系统行为（应自动暂停 + 页面提示，其他数据源不受影响）
- [ ] 演练降级场景：千问超时/失败重试，最终失败不生成无依据提醒（验收场景 6）
- [ ] 连续试运行 3 天，检查：调度任务准时率、提醒准确性、误报率、页面响应
- [ ] 八条端到端验收场景在云环境重新过一遍
- [ ] 交互查询联调：四类场景各 3 条测试问题，核查证据链完整性和额度计数准确性
- [ ] 邮件联调：P1 实时推送触发验证、合并窗口验证、日报定时生成验证、SMTP 失败重试验证
- [ ] 邮件风暴演练：模拟单小时 30 条 P1，确认限频和合并摘要生效

---

## 阶段 5：正式化（备案完成后）

- [ ] ICP 备案通过 → Nginx 切换 443 + 域名 + 免费 SSL 证书（Let's Encrypt 或阿里云免费证书）
- [ ] HTTP 强制跳转 HTTPS
- [ ] 安全组放行策略评审：是否仍需 IP 白名单，或改为全员可访问 + 强认证
- [ ] 天眼查正式 API/MCP 套餐切换（替换个人账号额度）
- [ ] 百炼免费额度到期前评估：转按量付费或购买节省计划（低至 4.5 折）

---

## 成本汇总（首年）

| 项目 | 过渡期（IP 方案） | 正式期 |
| --- | ---: | ---: |
| ECS 2核4G | ¥199 | ¥199（续费同价） |
| 域名 + SSL | ¥0 | ¥70~80 |
| 千问 API | ¥0（免费额度内） | ¥200~1,500 |
| 天眼查 | ¥0（个人免费额度） | ¥1,500~9,000（视套餐谈判） |
| OSS + 快照 | ¥50~100 | ¥100~200 |
| 邮件推送 | ¥0（企业邮箱 SMTP） | ¥0~50（量大才需 DirectMail） |
| **合计** | **约 ¥250~300** | **约 ¥2,100~11,000** |

## 关键风险清单

| 风险 | 应对 |
| --- | --- |
| 天眼查企业合同不含 API/MCP 授权 | 阶段 0.2 最先确认；降级为人工导出导入 |
| 个人免费额度违规用于生产 | 仅限开发期；正式期必须切换企业授权 |
| 免费额度用尽产生意外扣费 | 百炼开"用完即停"；代码层调用预算双保险 |
| 纯 IP 明文传输被嗅探 | 过渡期严格 IP 白名单；尽快切域名+HTTPS |
| 千问结构化输出漂移 | Pydantic 校验 + 重试 + qwen-max 备选 |
| 邮件风暴（P1 集中爆发刷爆收件箱） | 同事件去重 + 15 分钟合并窗口 + 单小时上限转摘要 |
| Agent 幻觉误导业务决策 | 只读工具白名单；回答强制附证据链；评分权留在规则引擎 |
| SMTP 凭据泄露或发送失败 | 授权码只存服务器环境变量；发送记录落库；失败页面告警 |
