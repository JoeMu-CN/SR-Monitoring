# 风险预警手机推送接入方案

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档状态 | 待评审（评审通过后按第 12 章实施） |
| 编制日期 | 2026-08-24 |
| 关联文档 | 技术方案.md（3.2 外部通知原为不含范围）、阿里云部署实施清单.md（1.6 邮件通知模块）、登录认证与角色权限生产上线规划.md |
| 适用阶段 | D10 验收后的 v1.1 扩展；与阿里云部署清单 1.6「邮件通知模块」同属外部通知能力 |
| 部署目标 | 本机 localhost 先行联调 → 阿里云 ECS（compose.prod.yaml） |

> 说明：技术方案 3.2 将"邮件、短信、企业微信等外部通知"列为 MVP 不含范围；本方案与 阿里云部署实施清单 1.6 相同，均为 v1.1 新增批准范围。不引入 Celery/Redis，复用既有 APScheduler 与 PostgreSQL。

## 2. 背景与目标

### 2.1 背景

当前风险预警链路为：信源采集 → 相关性过滤 → AI 解析 → 事件归并 → 供应商匹配 → 规则评分 → 生成 P1~P4 提醒，**止步于平台内展示**。风险提醒只在 Web 端可见，关键风险（P1/P2）无法实时触达责任人，存在以下缺口：

- 高风险事件发生后，责任人需主动登录平台才能发现，时效性差；
- 无分级触达（P1 全员、P2 采购负责人）与免打扰机制；
- 阿里云部署实施清单 1.6 已规划邮件通知，但邮件时效一般、可能进垃圾箱，缺少手机实时触达渠道。

### 2.2 目标

1. 将新增 P1（可配置含 P2）风险提醒实时推送到指定人员的**手机**。
2. 渠道：**钉钉/飞书机器人（主，群消息）** + **Server酱/PushPlus（辅，个人微信）**，短信/语音作为后续升级通道预留。
3. 与 1.6 邮件通知共享同一套通知内核（触发、订阅、防风暴、投递记录），渠道做成可插拔 Provider。
4. 零侵入现有风险引擎与调度分析链；防骚扰（去重、合并、限频、免打扰）为硬性要求。

### 2.3 不包含范围

- 短信、语音电话通道（仅预留 Provider 接口，本期不实现）；
- 移动端 App / 小程序（依赖太重，明确不做）；
- 风险处置闭环（确认、指派、跟进）——仍属后续规划，不在本方案；
- 邮件通知本体——按阿里云部署实施清单 1.6 另行实施，本方案只保证接口兼容。

## 3. 现状分析

### 3.1 相关数据模型（既有，复用不改）

| 表 | 关键字段 | 用途 |
| --- | --- | --- |
| `risk_events` | severity(critical/high/medium/low)、summary、event_type、facts | 风险事件本体 |
| `supplier_event_matches` | supplier_id、event_id、score、reasons、evidence | 供应商-事件匹配 |
| `risk_alerts` | match_id、level(P1~P4)、score、status(current/expired)、expires_at、created_at、updated_at | 风险提醒（推送的**数据源**） |
| `rule_dimension_configs` | key、enabled、config(JSONB) | 既有配置化模式，通知规则可沿用同思路 |

关键约束：`risk_alerts` 对同一 match（供应商×事件）仅保留一条 `current` 记录（唯一索引 `uq_risk_alerts_current_match_id`）；等级变化时原地更新 `level` 与 `updated_at`（engine.py `process_event`）。因此**推送去重以 alert 为粒度即可天然覆盖"同供应商同事件只推一次"**。

### 3.2 调度器结构（复用）

`scheduler/main.py` 已注册 collect / expire / cleanup / research 等 Job。本方案新增一个独立 `notify_job`（间隔扫描），不改动 `_process_pending_signals`、`process_event` 等现有逻辑，实现零侵入。

### 3.3 配置模式（复用）

`app/config.py` 使用 `os.getenv` + frozen dataclass 读取环境变量。通知配置沿用此模式，密钥只放 `.env` / 生产环境变量，不落库、不落日志。

## 4. 总体架构

```
scheduler 采集分析链（现有，不改）
        │ 生成/更新 risk_alerts (P1~P4)
        ▼
notify_job（新增，独立 Job，间隔 60s）
        │ 1) 游标扫描新增/升级 alert
        │ 2) 订阅过滤（级别/免打扰/渠道）
        │ 3) 防风暴（同事件去重 / P2 合并窗口 / 单小时限频）
        ▼
notification 服务（Provider 适配层，抽象 send()）
        ├── DingTalkWebhook    钉钉自定义机器人
        ├── FeishuWebhook      飞书自定义机器人
        ├── ServerChan         个人微信（sctapi.ftqq.com）
        ├── PushPlus           个人微信（pushplus.plus）
        └── [预留] AliyunSMS / AliyunVoice / SMTP(1.6 邮件)
        ▼
   投递记录 notification_deliveries 落库（幂等、可审计、失败重试）
        ▼
   目标人员手机（钉钉群 / 飞书群 / 个人微信）
```

设计要点：

1. **零侵入**：推送独立于风险分析链；`notify_job` 只读 `risk_alerts`，不修改引擎。
2. **Provider 适配层**：抽象 `send(channel, title, content, severity)`，新增渠道只需实现一个类并注册，业务逻辑不绑定单一厂商（延续平台既有"Provider 适配层"架构决策）。
3. **游标推进**：以 `risk_alerts.id` 为游标（自增主键），记录最后扫描到的 alert id 与时间，重启后可续跑。
4. **可审计**：每次投递写 `notification_deliveries`（渠道、接收人、内容摘要、状态、错误、关联 alert id），与 1.6 邮件发送记录同表结构。

## 5. 渠道接入详设

### 5.1 钉钉自定义机器人

- 地址：`POST https://oapi.dingtalk.com/robot/send?access_token={TOKEN}`
- 安全设置二选一：仅关键词 / 加签（推荐加签：`timestamp + "\n" + secret` 做 HmacSHA256，Base64 后拼 `&timestamp={t}&sign={s}`）。
- 消息体（文本）：
```json
{"msgtype": "text", "text": {"content": "【风险预警 P1】..."}}
```
- 配置项：`NOTIFY_DINGTALK_WEBHOOK_URL`（含 token）、`NOTIFY_DINGTALK_SECRET`（加签用，可空）、`NOTIFY_DINGTALK_ENABLED`。

### 5.2 飞书自定义机器人

- 地址：`POST https://open.feishu.cn/open-apis/bot/v2/hook/{WEBHOOK_ID}`
- 签名（可选）：`timestamp + "\n" + secret` 做 HmacSHA256（key=secret）→ Base64 得 `sign`，放入消息体。
- 消息体（文本）：
```json
{"timestamp": "1724472000000", "sign": "...", "msg_type": "text", "content": {"text": "【风险预警 P1】..."}}
```
- 配置项：`NOTIFY_FEISHU_WEBHOOK_URL`、`NOTIFY_FEISHU_SECRET`（可空）、`NOTIFY_FEISHU_ENABLED`。

### 5.3 Server酱（个人微信）

- 地址：`POST https://sctapi.ftqq.com/{SEND_KEY}.send`
- 表单格式：`title`（标题）+ `desp`（正文，支持 Markdown）。
- 说明：接收人需微信扫码关注「Server酱」公众号绑定 SendKey；服务为免费/捐赠制，**稳定性依赖第三方**，作辅助通道。
- 配置项：`NOTIFY_SERVERCHAN_SEND_KEY`、`NOTIFY_SERVERCHAN_ENABLED`。

### 5.4 PushPlus（个人微信）

- 地址：`POST http://www.pushplus.plus/send`（生产建议 HTTPS）
- JSON 格式：`{"token": "...", "title": "...", "content": "...", "template": "txt"}`
- 与 Server酱互为备选，实现其一即可，两者都实现成本一致。
- 配置项：`NOTIFY_PUSHPLUS_TOKEN`、`NOTIFY_PUSHPLUS_ENABLED`。

### 5.5 渠道优先级与降级

- 单条提醒按配置顺序投递：钉钉/飞书（主）→ 个人微信（辅）；任一渠道失败不影响其他渠道。
- 全部渠道失败：写投递记录 `status=failed`，进重试队列（最多 3 次，指数退避）；最终失败记入日志并保留记录，不静默丢失。

## 6. 触发与防骚扰策略

### 6.1 触发条件

| 场景 | 条件 | 动作 |
| --- | --- | --- |
| 新提醒 | `risk_alerts.status='current' AND id > 游标` 且 `level ∈ {P1, P2}`（阈值可配置） | 推送 |
| 等级升级 | 同 match 的 current alert `level` 提高（如 P2→P1） | 重新推送（内容标注"等级升级"） |
| 等级不变/降级 | 其余 updated_at 变化 | 不推送 |

### 6.2 防风暴（对齐 1.6 邮件模块语义）

| 机制 | 规则 | 实现 |
| --- | --- | --- |
| 同事件去重 | 同 match 只推一次（alert 粒度天然保证） | 投递记录 `alert_id` 唯一 |
| P2 合并窗口 | P2 提醒按 **15 分钟窗口** 合并为一条摘要 | 窗口内积压，窗口截止时发送汇总 |
| P1 即时 | P1 单条立即发送（不等待合并） | 扫描即发 |
| 单渠道限频 | 单小时上限 20 条/渠道，超限转合并摘要 | 投递记录按小时计数 |
| 免打扰时段 | 可配置（如 22:00–08:00 不推 P2，P1 仍推） | 订阅配置字段 |

### 6.3 订阅模型（第一阶段：配置化，非页面化）

沿用 `rule_dimension_configs` 的 `key=notification-subscription` 思路，配置 JSON 形如：

```json
{
  "enabled": true,
  "push_levels": ["P1", "P2"],
  "channels": ["dingtalk", "serverchan"],
  "quiet_hours": {"start": "22:00", "end": "08:00", "suppress_p2": true},
  "hourly_limit": 20,
  "merge_window_minutes": 15
}
```

- 第一阶段以 DB 配置 + `.env` 开关为主，不做页面；
- 页面化订阅管理（接收人、级别、渠道、免打扰）列入后续迭代，与 1.6 邮件订阅配置表合并实现。

## 7. 数据模型（新增）

### 7.1 `notification_subscriptions`（订阅配置，第一阶段可仅一行全局配置）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint PK | |
| channel | text | dingtalk / feishu / serverchan / pushplus |
| receiver | text | 群名或接收人标识（描述用途，不留敏感个人信息） |
| push_levels | jsonb | 订阅级别，如 ["P1","P2"] |
| quiet_hours | jsonb | 免打扰时段 |
| enabled | bool | 开关 |
| created_at / updated_at | timestamptz | |

### 7.2 `notification_deliveries`（投递记录，幂等 + 审计）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint PK | |
| alert_id | bigint FK→risk_alerts | 关联提醒（同 alert 仅一条成功记录） |
| channel | text | 渠道 |
| status | text | success / failed / retrying |
| title / content | text | 发送内容（脱敏后留存） |
| attempt | int | 重试次数 |
| error | text | 失败原因（不落密钥） |
| delivered_at | timestamptz | |
| created_at | timestamptz | |

约束：`UNIQUE(alert_id, channel)`（保证同提醒同渠道只投一次成功）；`UNIQUE(alert_id, channel, status)` 部分唯一不适用于 Postgres，改为应用层先查后插 + 唯一索引兜底。

> 与 1.6 邮件通知共用本表（channel='email'），邮件实现时不再建表。

### 7.3 Alembic 迁移

新增 `0021_notification_foundation`（两张表 + 索引），编号延续现有 0020。

## 8. 配置项（.env 新增）

```dotenv
# 通知总开关
NOTIFY_ENABLED=false
NOTIFY_PUSH_LEVELS=P1,P2
NOTIFY_SCAN_INTERVAL_SECONDS=60

# 钉钉
NOTIFY_DINGTALK_ENABLED=false
NOTIFY_DINGTALK_WEBHOOK_URL=
NOTIFY_DINGTALK_SECRET=

# 飞书
NOTIFY_FEISHU_ENABLED=false
NOTIFY_FEISHU_WEBHOOK_URL=
NOTIFY_FEISHU_SECRET=

# Server酱（个人微信）
NOTIFY_SERVERCHAN_ENABLED=false
NOTIFY_SERVERCHAN_SEND_KEY=

# PushPlus（个人微信，与 Server酱二选一）
NOTIFY_PUSHPLUS_ENABLED=false
NOTIFY_PUSHPLUS_TOKEN=

# 防风暴
NOTIFY_HOURLY_LIMIT=20
NOTIFY_MERGE_WINDOW_MINUTES=15
NOTIFY_QUIET_START=22:00
NOTIFY_QUIET_END=08:00
```

密钥仅存 `.env`（生产为环境变量/部署平台 Secret），不提交版本库、不打印日志。

## 9. API 设计

| 方法 | 路径 | 权限 | 说明 |
| --- | --- | --- | --- |
| POST | `/api/v1/notifications/test` | risk_admin | 向配置渠道发测试消息（标题+正文可自定义），验证链路 |
| GET | `/api/v1/notifications/deliveries` | risk_admin | 分页查询投递记录（渠道/状态/时间过滤） |
| GET | `/api/v1/notifications/subscriptions` | risk_admin | 查看当前订阅配置 |
| PUT | `/api/v1/notifications/subscriptions` | risk_admin | 更新订阅配置（级别/渠道/免打扰/限频） |

测试端点必须返回各渠道的实时发送结果（成功/失败原因），便于联调。全部挂 `/api/v1`，与既有路由风格一致。

## 10. 消息模板

P1 单条（文本版）：

```
【风险预警 P1】供应商「{供应商名}」
维度：{事件类型/子类型}
事件：{事件摘要（80 字内）}
匹配理由：{match reasons 前 2 条}
来源：{信源名 · 发布时间}
平台链接：{前端详情页 URL}
时间：{YYYY-MM-DD HH:mm}
```

P2 合并摘要（15 分钟窗口）：

```
【风险预警汇总】过去 15 分钟新增 {N} 条 P2 提醒
1. {供应商}｜{事件摘要截断}（{链接}）
2. ...
（详细请登录平台查看）
```

约束：

- 摘要截断、敏感信息（内部编码、密钥、完整供应商清单）不出现；
- 链接指向平台详情页，带有效 token 的链接一律不生成（跳转由登录态保证）；
- 模板常量集中定义，渠道差异（钉钉文本/Server酱 markdown）由 Provider 负责渲染。

## 11. 安全与密钥管理

1. webhook URL、SendKey、token 仅存 `.env` / 生产环境变量；日志与投递记录**不落密钥**。
2. 投递记录 `content` 字段仅存脱敏摘要，不存原始证据全文。
3. 测试端点与订阅管理接口限 `risk_admin` 角色（复用已上线的认证与权限矩阵）。
4. 出网调用统一走既有网络策略：仅允许配置的推送域名（oapi.dingtalk.com、open.feishu.cn、sctapi.ftqq.com、pushplus.plus），禁止任意出网。
5. 推送内容不含供应商内部采购金额、内部备注等敏感业务数据。

## 12. 实施步骤（评审通过后执行）

### 12.1 文件清单

| 文件 | 操作 | 内容 |
| --- | --- | --- |
| `backend/app/notification/__init__.py` | 新增 | 模块入口、Provider 抽象接口 |
| `backend/app/notification/providers.py` | 新增 | DingTalk/Feishu/ServerChan/PushPlus 实现 |
| `backend/app/notification/models.py` | 新增 | subscriptions / deliveries 表模型 |
| `backend/app/notification/service.py` | 新增 | 游标扫描、订阅过滤、合并窗口、限频、发送编排、重试 |
| `backend/app/notification/router.py` | 新增 | 测试发送 / 投递记录 / 订阅配置 API |
| `backend/alembic/versions/0021_notification_foundation.py` | 新增 | 建表迁移 |
| `backend/app/config.py` | 修改 | 新增 `NotificationSettings` dataclass（仅追加，不动既有配置） |
| `backend/app/scheduler/main.py` | 修改 | 注册 `notify_job`（interval 60s，`NOTIFY_ENABLED` 为 true 时注册） |
| `backend/app/main.py` | 修改 | 挂载 notification router |
| `.env.example` / `compose.prod.yaml` 注释 | 修改 | 新增配置项说明（不写真实值） |

**不改动**：`risks/engine/*`、`signals/*`、`research/*`、既有 scheduler jobs 逻辑。

### 12.2 顺序

1. 迁移建表 → 2. config 配置项 → 3. providers 实现 → 4. service 扫描编排 → 5. router + main 挂载 → 6. scheduler 注册 → 7. 测试端点联调 → 8. 真实事件验证 → 9. git 提交（详细 feat）。

### 12.3 备份

实施前 `git` 确认工作区干净；迁移前对数据库执行一次 `pg_dump`（沿用既有备份流程）。

## 13. 验证方式（验收标准）

| 编号 | 验证项 | 方法 | 预期 |
| --- | --- | --- | --- |
| V1 | 链路连通 | 调用 `/api/v1/notifications/test` | 钉钉群/飞书群/个人微信均收到测试消息，接口返回各渠道 success |
| V2 | 自动触发 | 用既有真实事件（江苏聚力/宁波鸿腾）重跑一轮分析 | 新生成 P1 提醒自动推送，内容含供应商/事件/链接 |
| V3 | 同事件去重 | 同一事件连续两轮分析 | 第二轮不重复推送 |
| V4 | P2 合并 | 15 分钟窗口内产生多条 P2 | 合并为一条摘要 |
| V5 | 限频 | 模拟单小时 >20 条 | 超出部分转合并摘要，投递记录计数正确 |
| V6 | 免打扰 | 配置时段内产生 P2 | P2 不推、P1 仍推 |
| V7 | 失败重试 | 临时改错 webhook URL | 记录 failed → 重试 3 次 → 最终失败可见可查，不静默丢失 |
| V8 | 权限 | 非 risk_admin 调测试端点 | 403 |
| V9 | 密钥安全 | 全链路日志 + 投递记录检查 | 无密钥、无敏感业务数据外泄 |
| V10 | 回归 | 既有测试集全量跑一遍 | 全绿（推送模块零侵入既有链路） |

## 14. 风险与回退

| 风险 | 影响 | 应对 / 回退 |
| --- | --- | --- |
| 钉钉/飞书 webhook 被滥用或群配置变更 | 推送中断 | 失败重试 + 投递记录可见；`NOTIFY_*_ENABLED=false` 即时停用该渠道 |
| Server酱/PushPlus 第三方不稳定 | 个人微信收不到 | 定位辅助通道；主通道为钉钉/飞书，且预留短信/语音升级 |
| 误报轰炸 | 责任人疲劳 | 防风暴机制兜底；推送阈值（P1/P2 可配）、免打扰可调 |
| 扫描游标错乱 | 漏推/重推 | 游标持久化在投递记录表（以 alert_id 单调推进），重启续跑；误推靠同事件去重兜底 |
| 配置错误（webhook 填错） | 发往错误目标 | 测试端点先行验证；仅支持 `risk_admin` 配置 |
| 与 1.6 邮件通知重复建设 | 双套逻辑 | 本方案与邮件共用 Provider 抽象与投递记录表，邮件实现只新增 SMTP Provider |

**回退方案**：整体关闭 `NOTIFY_ENABLED=false` 即完全回到现状（仅平台内展示），无代码回滚需求；迁移可安全回滚（`alembic downgrade`）。

## 15. 成本估算

| 项目 | 成本 | 说明 |
| --- | --- | --- |
| 钉钉/飞书机器人 | ¥0 | 群机器人免费 |
| Server酱 | ¥0（免费额度） | 免费/捐赠制；重度使用再评估 |
| PushPlus | ¥0（免费额度） | 免费额度内足够 |
| 短信/语音 | ¥0（本期不实现） | 后续若接入：短信约 4-5 分/条，语音约 1 毛/通 |
| 邮件（1.6 规划） | ¥0（企业邮箱 SMTP） | 与阿里云部署清单 1.6 一致 |
| **本期合计** | **¥0** | 无新增云资源，仅代码与配置 |

## 16. 后续扩展

1. **短信/语音通道**：实现 `AliyunSMS` / `AliyunVoice` Provider，仅用于 critical/P1 兜底（约 4-5 分/条）。
2. **订阅页面化**：订阅管理从 DB 配置升级为前端页面（接收人、级别、渠道、免打扰），与 1.6 邮件订阅配置表合并。
3. **与邮件通知合并落地**：按阿里云部署实施清单 1.6 实现 `EmailNotifier`，复用本方案的 Provider 抽象、投递记录与防风暴逻辑。
4. **投递统计**：基于 `notification_deliveries` 出渠道成功率与误报率看板。
